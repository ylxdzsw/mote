# Development notes

## 2026-09-07 — First demo and scaffold

User confirmed that spatial objects are relative to anchors, and that the draft should be cached in browser-local datastores. The proposed stack was React + TypeScript + Vite with Tiptap/ProseMirror for rich text; implemented as a single-document browser application, without a backend.

The initial demo combines semantic paragraphs/phrases, a central theme, explicit vertical spacers, and anchored rich-text boxes. Main and floating text share the same semantic schema and theme. Native rich-text paste is constrained by the schema; no arbitrary inline CSS is retained.

V0 uses stable main-block UUIDs and floating-object anchor IDs plus document-pixel offsets and width. Anchor geometry is derived after layout and never persisted. If an anchor is deleted, retain its boxes and reanchor to the nearest surviving predecessor (or first remaining block). User confirmed this deletion behavior on 2026-09-07.

IndexedDB holds one current draft, including theme and spatial data. Writes are local document persistence, intentionally distinct from HTTP caching, which is disabled. Browser storage is not guaranteed durable and is not a backup. No cloud, import/export, document library, or migrations. Multi-tab editing is not coordinated yet.

Mobile means a viewport below 768px in this demo and forces reading mode. Reading scales the fixed-width page as a unit to retain the spatial composition. Desktop defaults to editing; document width starts at 800px. Width changes preserve offsets and do not automatically rearrange boxes. These responsive/width policies are implementation defaults to discuss further.

Undo/redo is per text editor (including main-text spacers); a unified document-wide history for theme/spatial changes is deferred. Reset and floating-box deletion ask for confirmation. Dependencies are locked with npm; no Git repository has been initialized.

## 2026-09-07 — Initial static deployment

User requested `mote.ylxdzsw.com` deployed through the existing Nginx and Cloudflare origin, with no authentication and no HTTP caching before release. It is a purely front-end static site; IndexedDB remains browser-local and no cloud service is involved. The deployment uses the existing wildcard origin certificate, a dedicated Nginx server block, and `Cache-Control: no-store` on all responses.

## 2026-09-07 — Document viewport and PPT-style box interaction

User requested page-level scrolling/zooming replaced by document-level interaction, chrome about 30% shorter, full-page-width transparent spaces with only selected top/bottom 1px borders, and removal of the anchor handle in favor of PPT-style border dragging. Desktop header/toolbar heights are now 50/37px (previously 72/53); mobile header is 45px (previously 64).

The app frame is viewport-bound. Canvas and inspector scroll independently; wheel/pinch and keyboard zoom gestures scale only the document, with explicit canvas zoom controls as well. Zoom is transient view state, not V0 document data. Entering a different editing/reading mode resets zoom to that mode’s baseline; reading fits the width, and users can zoom further. Document-pixel box offsets and widths remain independent of view scale.

Spaces extend through both text margins, but logical anchor origins stay at the main-text left margin so changing the spacer’s visual width does not shift anchored objects. No space label, fill, side border, or unselected decoration remains. Floating boxes retain their anchor model internally without an anchor marker or move handle. Drag their border/padding to move; text remains directly editable, and resize/delete controls appear only on selection. Border focus supports keyboard movement and deletion.

## 2026-09-07 — Version control

User requested Git initialization, a commit of the current project, and deployment. The initial repository snapshot includes the V0 application and document-viewport interactions. Build output, dependencies, environment files, and local agent sessions/objects are excluded; project guidance and development notes are tracked.

## 2026-09-08 — Visual-scrollbar minimap and global view settings

User chose faithful page proportions as the default minimap behavior, with an optional whole-document fit mode if they share implementation. “Previewable” means content visible inside the strip, explicitly no hover popup. Navigation is vertical only, preserving horizontal scroll even when zoomed in. Small screens, especially mobile reading, hide it by default.

Both modes share an inert snapshot of the actual rendered sheet, preserving wrapping, semantic theme, spacers, and floating-box composition without extra Tiptap instances. Proportional mode scrolls the miniature as the document scrolls; fit mode compresses the vertical axis only when needed, never stretching short notes. The overview includes reachable right-side overflow in its width and uses canvas scroll extent for vertical mapping; the app footer itself is not drawn. Unreachable negative overflow remains an existing canvas limitation. Zoom affects the viewport indicator rather than the document miniature's scale. Pointer navigation preserves editor focus/selection; keyboard access uses the scrollbar itself. Native scrolling remains available.

Global settings temporarily live in the right sidebar, separate from document/theme settings and persisted in localStorage (`mote-view-settings`), not V0 or the document undo history. Visibility is Automatic/Show/Hide; Automatic shows only above 1050px viewport width. Explicit Show also works on mobile. Reading mode has a View button for the global-settings sidebar, overlaid rather than squeezing the canvas on mobile. A full configuration dialog remains future work.

The initial renderer refreshes its DOM snapshot on content/layout changes, coalesced per animation frame; scroll-only work translates the existing snapshot and viewport overlay. It duplicates static layout, not editor instances, but is not a tiled/virtualized renderer for extremely large documents.

Deployed after production-build and scoped Chromium checks, including a 60-section spatial note, theme/width reflow, live box dragging, keyboard/pointer navigation, and mobile opt-in/default hiding. Assets were copied before atomically replacing the live HTML; previous assets were retained for already-open clients, with a rollback copy at `/tmp/mote-before-minimap.VTYgSm/site`. Public HTML/JS/CSS matched the build and returned `Cache-Control: no-store` (Cloudflare DYNAMIC/BYPASS). Desktop and fresh mobile reading were smoke-tested on the live site.

## 2026-09-08 — Left-side minimap, no native canvas scrollbars

User requested the minimap moved to the left and the native scrollbar removed. The canvas now hides both native scrollbar tracks without disabling scrolling, including when the minimap is hidden on small screens. Wheel/touch/trackpad scrolling and minimap navigation remain; settings-panel scrollbars are unchanged. The minimap stays in its own left column, and zoom controls stay at the canvas's bottom right. This supersedes the initial decision to retain a visible native canvas scrollbar.

## 2026-09-08 — Header zoom, heading bookmarks, longer example

User requested the scale indicator in the top bar between saving and mode selection, small bookmark-like heading labels to the right of the minimap, and a demo three times longer. The full zoom control group now lives in a header portal, keeping document zoom ownership and focal-point behavior inside the canvas. Mobile keeps compact zoom controls and an accessible save message represented visually by its status dot.

Bookmarks cover every semantic `heading`, including floating-box headings, ordered by measured vertical position; `title` remains a separate semantic class. A dedicated narrow lane to the right of the miniature avoids covering document content. Labels track the same geometry in both sizing modes, preserve text selection/horizontal scroll when clicked, and highlight the last heading passed at the top of the viewport. Close labels are spread vertically without changing their targets; unusually dense visible headings can scroll within the bookmark lane. Headings outside a scrolling proportional miniature are hidden until their region appears. Bookmarks are derived view state, not saved document data.

The example now has three composed sections, 27 main blocks instead of nine, and three anchored notes instead of one, measuring about 2700px instead of 900px at the default theme/width. Existing IndexedDB drafts are never replaced by an updated example; users can load it through the existing confirmed Reset to example action.

Reading-mode Fit remains reachable below the normal 25% zoom floor when the available canvas is very narrow (for example, explicitly showing the minimap and bookmarks on mobile). The effective lower limit is the smaller of 25% and the reading fit scale.

Deployed after build and scoped browser verification, including a 71-heading long-document fixture, responsive header checks, and preservation of existing drafts. Live desktop reports 27 main blocks, three floating notes, 11 heading bookmarks, and 2699px page height; live mobile retains header zoom and hides the minimap by default. Public HTML/JS/CSS matched the build with no-store headers. Rollback copy: `/tmp/mote-before-heading-bookmarks.igKw0C/site`.

## 2026-09-08 — Main-text bookmarks and an inset reading focus

User requested earlier current-heading activation, main-text-only labels, half-width labels, and label text reduced by 1px. The activation line is now 15% down the canvas viewport, capped at 120 CSS pixels, independent of document zoom. Bookmark clicks align the heading to the same line so the selected destination becomes current without first reaching the top edge. Only semantic headings inside the main text contribute labels; floating content remains visible in the miniature but never contributes bookmarks or current-heading selection. The label lane is halved from 128px to 64px on desktop and from 88px to 44px on mobile, with 9px text (previously 10px), tighter padding, and full text retained in tooltips/accessibility names. These decisions supersede the initial floating-heading inclusion and near-top activation behavior.

Built, browser-checked, and deployed: activation switches on either side of the 120px line, short viewports use the 15% offset, bookmark jumps land on that line, both sizing modes retain eight main-text labels for the demo, floating notes remain drawn, and explicit mobile minimaps use 44px labels with 9px text. Live assets match the build with no-store headers. Rollback copy: `/tmp/mote-before-compact-bookmarks.AR7deK/site`.

## 2026-09-08 — Zoom presets and overlay bookmarks

The current zoom percentage is now a native preset dropdown (25%, 50%, 75%, 100%, 125%, 150%, 200%, 300%), replacing the separate reset button. Reading mode keeps Fit inside the dropdown; continuous zoom and Ctrl/⌘ 0 remain available. Arbitrary gesture/button scales still display their current rounded percentage.

Bookmark labels no longer reserve horizontal space: only the miniature strip has a layout column, and the document centers in the remaining canvas without considering label width. The existing 64px/44px labels overlay that canvas and may cover the document. Only actual labels intercept pointers; gaps pass through to the document. Dense labels remain independently wheel-scrollable. This supersedes the earlier dedicated non-overlapping bookmark lane decision.

## 2026-09-08 — A zoom detent at actual size

Incremental zoom (wheel/trackpad, pinch, header +/−, keyboard +/−) snaps to exactly 100% when its target is 95–105%. Wheel gestures accumulate their unsnapped scale, restarting after a 250ms pause, so continued small deltas can move past the detent rather than getting trapped at 100%. Pinch keeps its existing gesture-start reference for the same reason. Explicit presets, reading-mode Fit, and Ctrl/⌘ 0 remain exact and bypass snapping so a near-100% fitted page never becomes too wide.

## 2026-09-08 — Floating images and directly editable tables

User requested additional floating objects, specifically images and directly editable tables, with all table text treated as body paragraphs while retaining inline semantic classes. Text, image, and table now share the existing anchor-relative geometry and border drag/width resize/delete interactions. Images can also be dragged by their visible content. The example keeps its 27 main blocks and three notes, adding a self-contained landscape illustration and a three-row/two-column table alongside the later notes. Existing drafts remain intact; confirmed reset loads the new composition.

Tables use a separate Tiptap schema with one table and paragraph-only cells. Paragraphs have no semantic-class attribute and always render as Body; even pasted headings and HTML header cells normalize to body. The shared emphasis/term mark is retained. ProseMirror's existing table package supplies cell selection, paste normalization, navigation, and row/column commands without adding dependencies. Tab/Shift+Tab navigates cells and leaves the editor at its boundaries; row/column controls are in the sidebar and preserve at least one row and column. Table structure participates in that editor's undo history, while object geometry/creation/deletion remains outside it.

Images are local files embedded as data URLs in IndexedDB, not remotely uploaded. Accept PNG, JPEG, WebP, GIF, and AVIF up to 10 MB and verify decoding before changing the document. Replacements preserve geometry and description; resizing preserves the intrinsic aspect ratio. The sidebar exposes an editable alt description. Asynchronous file reads are scoped to the originating document/target so reset cannot insert an old pending image into a new example. Image replacement, like other spatial changes, is outside editor undo. HTTP no-store and mobile reading-only policies remain unchanged.

Production-preview smoke checks covered insertion of all three kinds, cell typing/marks/navigation, row/column commands and last-cell safeguards, constrained rich-text/table paste, select-all deletion and history, shared body theme, a 35-row table extending the page, image validation/replacement/description, scaled pointer dragging (150% image, 75% table), keyboard geometry, confirmed/cancelled deletion, anchor reflow/deletion retention, IndexedDB reloads, and simulated quota-error/retry. Both minimap modes and 1440/768/390/320px layouts were checked, including read-only interaction and mobile opt-in minimaps. A stale toolbar state on switching from a table to an image was found and fixed; minimap snapshots also strip cell-selection decoration.

Built and deployed to mote.ylxdzsw.com. Public HTML/JS/CSS match the final build and return no-store with Cloudflare DYNAMIC/BYPASS. Live checks confirm the five-object/27-block example, direct table edits with inline marks and added rows surviving reload, image selection clearing text tools, and a fresh mobile session fitting the page with all editors read-only. No browser console errors were observed. TypeScript/build and diff whitespace checks pass; Vite reports only its bundle-size advisory (about 580 kB JS, 179 kB gzip). No new dependencies or regression suite were added.

## 2026-09-08 — Border-only floating controls and resizable main-text segments

User requested removal of floating delete buttons and corner resize handles: top/left/bottom borders now move each object, while the entire right border resizes its width. Images no longer move when dragged through their interior; clicking inside only selects them. Direct text/table editing remains unchanged. Keyboard movement and confirmed Delete/Backspace on a focused object remain available; the right border itself is focusable for keyboard width adjustment. No discrete resize glyph or delete control is rendered. This supersedes the prior image-content dragging and selected-object button behavior.

A main-text segment is a continuous paragraph run between spaces, not an individual paragraph. One horizontal border is shared by each pair of adjacent sections. Dragging it resizes the section above and moves all subsequent content in the normal flow, rather than redistributing a fixed total height between neighbors. Spaces keep their existing 24px floor. Text starts automatic; a dragged height larger than natural content becomes a saved minimum. Content can shrink without losing that reserved room. Once natural content meets/exceeds the minimum (within 0.1 document pixels), or the user drags back to content height, clear the minimum so later shrinking is automatic again. Theme/page-width reflow also counts as natural content growth. Border Up/Down and Home provide keyboard resizing/reset, and reading/mobile views have no interaction overlays.

Keep the flat main editor schema and existing anchor UUIDs. The leading segment's `minSegmentHeight` belongs to the editor document node; later segment minimums belong to their preceding spacer, alongside that spacer's separate `height`. This ownership avoids losing a minimum when a segment's first paragraph is deleted or replaced. Inserting a space starts a new automatic trailing segment; deleting a space joins runs under the earlier minimum, releasing it if combined content already reaches it. Nonexistent/empty runs clear unused minimums. The minimum includes normal paragraph spacing. Extra room is rendered as measured padding on the run's last paragraph, not as a fixed content height.

Drag previews are ProseMirror decorations and never persist intermediate document mutations. Commit once on release as an isolated editor-history step; Escape, pointer cancellation, mode changes, and zoom changes cancel previews. Automatic minimum release is saved without adding a separate undo event. Spacer previews use a distinct CSS variable from stored spacer height: smoke testing caught that overriding/removing the same inline height property could otherwise erase the rendered height on commit. Main-anchor measurement observes individual blocks as well as the whole text; minimap snapshots retain layout padding but omit all border controls.

Smoke checks passed for each floating object's four borders, image-interior selection, keyboard width/deletion controls, text and space dragging at 75%/150%, clamp-to-content, rounding tolerance, growth/release/subsequent shrink, paragraph insertion/deletion, same-size paragraph replacement, consecutive spaces and segment joins, Escape cancellation, undo/redo, no-op clicks, reload persistence, width reflow, minimap cleanup, and reading/mobile layouts. Layout decoration state is invalidated on document changes because replacing a paragraph can discard its decoration even if positions and desired padding remain identical. Settled layouts were checked for zero repeating transactions. Empty paragraphs still count as a text run and retain minimums; only runs with no paragraphs discard their unused minimum.

Built and deployed. Live desktop has seven shared section boundaries, five floating objects without buttons, persisted text minimums/spacer heights, and working right-border keyboard resizing. A fresh 390px session is read-only and fitted without resize controls. No browser console errors were observed. Public HTML/JS/CSS match the final build, with Cache-Control no-store and Cloudflare DYNAMIC/BYPASS. TypeScript/build and whitespace checks pass; only the usual Vite bundle-size advisory remains (about 586 kB JS / 181 kB gzip).

## 2026-09-08 — Floating repulsion design discussion (not implemented)

User wants per-object Overlap / Repel behavior. Repellers affect only main-text lines; floating text, images, and tables are opaque rectangular obstacles and never repel one another. Each main-text line retains only its leftmost usable interval: wrap before the leftmost intersecting repeller and never continue in free space to its right. Front-to-back stacking is independent of this text-flow behavior.

User chose absolute document-space x and nonnegative anchor-relative y, superseding the current text-margin-relative x and unrestricted y design. Before the first main block, use a document-top origin, represented either by a null anchor or a virtual top anchor; representation remains to be finalized. User favors automatic reanchoring when moving objects. Proposed interaction: derive attachment from placement on explicit movement, preserve visual position when changing anchors, and retain attachment during ordinary text/theme reflow. Preserve the anchor block's logical top while repulsion changes its internal line layout and height. Forward-only exclusions avoid backward layout dependencies; any above-object gap must also respect the anchor boundary. Implementation and remaining defaults are still under discussion.

Future work requested by user: snapping during floating-object placement, especially alignment/snapping between floating objects. Record for later design; do not include snapping in the initial repulsion implementation.

Implemented the initial repulsion slice. Floating geometry now uses absolute document-space x, nonnegative anchor-relative y, and nullable document-top anchors. New and example objects explicitly default to Overlap; the inspector can switch any selected object to Repel. Repellers use transient paragraph-local CSS exclusion proxies and native line breaking; they affect the anchored block and following main text, never floating objects, and preserve the anchored block's top. Dragging keeps the object under the pointer and automatically commits a new anchor plus offset based on final document placement. Repulsion previews remain transient and do not release saved segment minimums. Production build and live deployment completed with no-store responses; native wrapping, document-top reanchoring, inspector control, existing draft loading, and no idle transaction loop were browser-checked.


## 2026-09-08 — Live document configuration and shared undo

User chose a fixed semantic vocabulary for now (Title, Heading, Body, Caption; Emphasis and Term), a right-side configuration panel, and immediate undoable edits rather than an Apply/Cancel preview. The Document header button opens Layout/Theme tabs in place of the selection inspector; selecting content does not close or retarget it. Edit class style opens the current paragraph/phrase class. View preferences have their own header entry, remain browser-local, and never enter document history. Mobile stays reading-only.

Layout has continuous page width (800px default, 320–2400px) and left/right main-text margins (55px default). Linking is a panel convenience, initially enabled for equal margins; enabling it aligns the right margin to the left, bounded to retain 120px of main text. At least 120px remains usable at every width. Top/bottom padding remains fixed. Margins and width reflow main text but do not rewrite floating objects’ absolute x or saved attachments.

Theme Defaults provide a single inheritance baseline rather than class-to-class inheritance. Paragraph classes store partial overrides for typeface, size, weight, color, line height, letter spacing, and before/after spacing. Defaults also own page background. Phrase overrides cover text color, transparent-or-colored background, weight, italic, and decoration; absent phrase typography follows the surrounding paragraph, not Body or Defaults directly. Tables continue to use Body exclusively. Each override can reset to inheritance; resetting a class clears all its overrides in one undo step. Existing browser drafts retain their content and explicit styles when the new baseline/margins are filled in.

A single chronological, session-local history replaces editor-local ProseMirror history. It covers text/class application, table structure, segment/spacer sizes, theme/layout, and floating-object content/geometry/creation/deletion/image changes. Up to 100 document snapshots share unchanged data; images are not cloned or serialized for each history comparison. Continuous typing groups within 750ms and breaks on selection/focus changes. Numeric input focus sessions and color-adjustment sessions group until the control is left; released border drags commit once. Native undo remains available inside text/numeric settings fields; toolbar Undo/Redo or shortcuts outside those inputs operate on the document. View changes are excluded. Reload and confirmed Reset to example clear history.

Layout normalization (especially automatic minimum-height release) folds into the triggering document change and does not clear redo. Undo restores both the configuration and affected segment minimums. Snapshot synchronization must restore root document attributes as well as child content: Tiptap setContent alone does not restore the leading segment’s minimum. Editor selections are restored without stealing focus; removing a selected object through undo returns the inspector to main text. There is no transient configuration preview and no separate commit operation.


Verification covered immediate shared main/floating/table typography, inherited versus explicit properties and reset undo, phrase inheritance from surrounding paragraphs, grouped numeric/color sessions, linked/asymmetric margins, width changes preserving floating x, chronological text/theme undo after panel closing, restored segment minimums after font-size and width reflow, table row/cell edits, floating creation/deletion/movement, and no idle document mutations. Reload retains configuration and clears only session history. Production preview and live checks passed; desktop/tablet headers fit, and 390px/320px mobile remains reading-only with View preferences available. Build/type checks and diff whitespace checks pass; no browser errors observed. Deployed assets match the build, with no-store HTML/JS/CSS and Cloudflare DYNAMIC/BYPASS. The existing bundle-size advisory remains (about 601 kB JS / 186 kB gzip); no dependencies or regression suite added.


## 2026-09-09 — Atomic space selection, Save no-op, Code and independent List blocks

User requested immediate stable space selection, suppressing the browser Save shortcut, a Code paragraph/block, and unordered List paragraphs that do not belong to containers. The space flicker was reproduced as a browser text selection in the following heading on pointer-down, followed by the intended spacer NodeSelection only on pointer-up. Intercept spacer pointer-down, prevent native mouse selection, and select/focus the spacer immediately; do not mask or delay toolbar updates. Ctrl/⌘S is suppressed at window capture in all modes and settings fields, with no document action; automatic local saving is unchanged.

Extend the fixed semantic vocabulary with Code and List, both still flat paragraph nodes with stable IDs. List stores a zero-based listLevel attribute, not nested list/list-item structures. Enter creates a same-level item, even for an empty item; the original retains its ID and the new item gets a new one. Shift+Enter stays in the same item. Tab/Shift+Tab and toolbar arrows change only explicitly selected items' indentation, without moving implied descendants or neighbors. Backspace at an item's start outdents once, or converts a top-level item to Body while retaining text. Every level uses the same List theme typography. Indentation is 24 document pixels per level; the top marker is solid and every sublevel marker hollow. List spacing is a class property (6px after by default), never inferred from adjacency or membership.

Code is plain unmarked text and soft breaks rendered as pre/code, with monospace added to the theme typeface choices and used as Code's default (15px, 1.55 line height). Enter/Shift+Enter add a newline inside Code, Tab inserts two spaces, and Ctrl/⌘Enter starts Body after the block. Backspace at its start converts to Body. These are initial Code interaction defaults; no syntax highlighter or language picker. Literal newline text normalizes to hardBreak nodes, so converting Code back to Body retains line breaks. Code conversion/paste strips phrase marks; its phrase-class control is disabled. Code and List apply to main and floating text only: the prior Body-only table constraint remains.

Clipboard unordered lists flatten into independent paragraph items with indentation levels; ordered lists become Body paragraphs rather than introducing ordered-list semantics. A structured Code/List paste replacing an empty or wholly selected paragraph adopts the incoming first block's class instead of losing it to ProseMirror's open-slice merge. Code paste takes plain text, preserving whitespace. Existing drafts gain missing Code/List theme definitions without replacing content or existing styles. Both new classes share existing segment sizing, forward repulsion, minimap rendering, document undo, and browser-local persistence.


Verified the before/after space-click transaction trace: the old heading TextSelection on mouse-down is gone, with only the spacer NodeSelection from pointer-down through release. Scoped browser checks covered list splitting at a cursor, empty-item Enter, stable/new IDs, independent level changes, soft Enter alignment, Backspace/outdent and undo, unordered clipboard flattening, ordered-to-Body normalization, Code whitespace/mark stripping/class conversion, main/floating theme sharing, Body-only tables, repulsion, reload persistence, and absence of idle document mutations. Ctrl/⌘S is prevented in editors, configuration fields, and mobile reading without changing content. Production preview and live smoke checks passed; 768px headers fit and 390px/320px remain reading-only. Build/type and whitespace checks pass, no browser errors observed, and no dependencies or regression suite were added. Deployed HTML/JS/CSS match the final build and retain no-store with Cloudflare DYNAMIC/BYPASS; only the existing bundle-size advisory remains (about 608 kB JS / 188 kB gzip).

## 2026-09-09 — Segment-local hover borders

Page hover no longer reveals every text/space boundary. Only the hovered segment's shared top/bottom boundaries appear, including hover in the page's text margins; floating objects do not reveal underlying segment borders. Selection alone no longer keeps space borders visible. Keyboard-focused and actively dragged boundaries remain visible for interaction feedback. Boundary positions are snapped to the zoom/device-pixel grid so fractional text layout does not make one space edge appear thicker than the other; document geometry remains unchanged.

Build/type checks and scoped Chromium checks passed for text/space hover, pointer exit, matching 1px lines, and spacer border dragging. Redeployed the demo; live hover works, public HTML matches the build, no browser errors, and no-store/DYNAMIC headers remain in place. The existing bundle-size advisory remains.

## 2026-09-09 — Floating hover reveals anchor segment

Hovering a floating text box, image, or table now reveals the boundaries of the segment containing its anchor block, not the segment geometrically beneath the object. This supersedes the previous exclusion of floating objects from segment highlighting. Every paragraph in a continuous text run maps to that same segment; spacer anchors map to their space segment. Document-top/null anchors have no segment to highlight. The object's own hover border is unchanged.

Build/type and whitespace checks passed. Browser checks covered all three floating kinds, pointer exit, a text anchor in a different segment from the object's position, and null anchors. Redeployed and confirmed live floating hover, matching public HTML, and no-store/DYNAMIC headers.

## 2026-09-09 — Softer segment boundaries

Segment boundary lines now render at 40% opacity instead of fully opaque green, retaining equal 1px top/bottom lines and the existing 8px resize hit area. Hover/anchor mapping is unchanged. Build and live browser checks passed; deployed with no-store headers.

## 2026-09-09 — No automatic document bottom margin

Removed the main text's 96px bottom padding, the 900px page-height floor, and the 64px trailing allowance below floating objects. The page now ends at the last main block or lowest floating object's bottom, with only its border remaining. Top padding, explicit spacer nodes, user-reserved text-segment heights, and the footer outside the document are unchanged. This supersedes the fixed bottom-padding decision.

Build/type and whitespace checks passed. Browser checks confirmed no trailing padding on the example, an 83px short document rather than a forced 900px page, and a low floating object extending the page without extra bottom space. Mobile remains read-only and contains the floating object. Redeployed and confirmed live bottom geometry, matching public HTML, and no-store/DYNAMIC headers.

## 2026-09-09 — Stable viewport while editing a reserved final segment

Reproduced before modifying code: drag the example's final text boundary down 80px, put the caret at the end, and scroll to the bottom. Enter temporarily dropped the paragraph padding and shrank the page by 33px; Backspace/join dropped it by 80px. Chromium clamped scrollTop from 1988 to 1955/1908 before ProseMirror's selection scrolling ran. The next animation-frame measure restored the same final document height but not the lost scroll position. This was transient node-decoration loss during split/join, not intended caret scrolling or minimum release.

The main editor now also retains the final segment's measured minimum at its container level, independent of replaceable paragraph decorations. This prevents temporary document collapse while padding is recalculated, without restoring scroll after the fact or suppressing normal caret scrolling. The floor follows resizing and is removed when the final segment returns to automatic sizing; no automatic bottom margin or page-height floor was reintroduced.

The original insert/delete flow now keeps scrollTop 1988 and scrollHeight 2801 throughout. Checked soft line breaks, paragraph splits/joins at 75%/150%, content growth releasing the minimum, subsequent automatic shrinking, Home reset, and zero idle transactions. Type/build and whitespace checks passed; live drag-and-edit reproduction also remains stable. Deployed HTML matches the build with no-store/DYNAMIC headers; no browser errors.

## 2026-09-09 — Relative theme lengths, inline classes, and inspector style actions

Removed the branding footer below the document. The document retains its content-sized bottom; surrounding canvas padding is unchanged.

Theme font sizes, letter spacing, paragraph spacing, and line heights now support em/px selection. Every em explicitly resolves against Defaults font size, including spacing and line height—not against the local paragraph font. The Defaults font size remains an absolute pixel baseline (17px initially) to avoid a circular reference. Line height additionally supports the existing unitless × local-font multiplier. Unit changes preserve the current rendered length and participate in shared history. The new presets use em for all applicable lengths (Title 3em, Heading 1.5em, Body/List 1em, Caption .75em, Code .875em), with readable proportional line heights/spacing. Existing numeric lengths keep their pixel meaning, except numeric line heights remain multipliers; existing custom themes are not replaced. CSS variables resolve theme lengths to document pixels centrally so main text, floating content, table Body text, previews, and minimap share identical default-relative semantics.

Emphasis is replaced by Primary (green) and Secondary (blue); Term remains. Bold is another semantic inline class whose preset only sets weight 700, without a color/highlight override. The single-inline-class model remains: switching to Bold replaces another inline class rather than nesting direct formatting. Ctrl/⌘B toggles it in main/floating/table text and rectangular table selections; Code consumes the shortcut without formatting. Pasted strong/b maps to Bold. Saved Emphasis marks/styles become Primary, retaining customized colors and content.

Removed the toolbar's combined style-edit button. The selection inspector now has separate Paragraph style… and Inline styles… actions, preserving the editor selection. Paragraph opens its actual class, or Body for table cells; inline opens the active class or Primary for plain text. Nontext/image/space selections disable both; Code disables only inline. The existing panel close action returns to the inspector, and selecting content while theme settings are open still does not retarget the panel.

Verified px/em and line-height × conversions, default 17→20 scaling (including spacing, floating headings, table Body and preview), unit undo/redo and inheritance reset, primary/secondary colors, Bold toggle/undo/plain appearance, code exclusion, floating/table and rectangular-cell Ctrl+B, bold HTML parsing, inspector selection retention and targets, reload persistence, and old Emphasis draft loading. 390px/320px remain reading-only with no footer. Build/type and whitespace checks pass; no browser errors. Deployed HTML/JS/CSS match the final build and retain no-store with DYNAMIC/BYPASS. Live checks confirm em presets, footer removal, Ctrl+B, and inspector style routing. No new dependencies or regression suite; the existing bundle-size advisory remains.

## 2026-09-10 — Preset typography and class-specific labels

The unmarked phrase option is now Plain. Inspector style actions name their actual target class (for example, Heading style… and Bold style…); routing is unchanged, including Primary for plain text and Body for tables. Theme Defaults now start at 16px with 1.5em line height, superseding the earlier 17px/1.7em baseline. Class overrides and saved draft themes remain unchanged; new documents and Reset to example use the revised preset.

Build/type and whitespace checks passed. Scoped browser checks verified Plain, dynamic Heading/Bold labels and matching theme targets, and rendered Body typography of 16px/24px. Redeployed the demo and confirmed live labels, typography, matching HTML, and no-store HTML/JS responses. No browser errors; the existing bundle-size advisory remains.

## 2026-09-10 — Column geometry, alignment, and the Table paragraph class

User confirmed that internal divider dragging preserves total table width and that the new paragraph class is named Table, independent of Body except for its preset. Table is structural and table-only: it never appears in normal paragraph-class choices, cells show a fixed Table toolbar label, and Table style… opens its global theme class. Like every paragraph class, Table inherits from Defaults, not Body. Its preset is `{ size: '1em' }`, identical to Body's. Existing Table-less drafts receive this preset rather than copying customized Body overrides. This supersedes all earlier Body-only table decisions.

Column layout is table-node metadata: normalized proportional widths plus left/center/right alignment. Internal dividers adjust only the neighboring pair; the object's outer right border scales the entire table and retains column proportions. Drag previews modify only the view, commit once on release, and cancel on Escape, pointer cancellation, zoom/mode changes, or document replacement. Keyboard dividers move by 8 document pixels (Shift for 1px), with a 40px minimum where the pair has enough space. No column-resize controls appear in reading mode or the minimap. New columns start left-aligned with equal-share weight; add/delete renormalizes surviving proportions. Column alignment targets the caret's column or every column intersecting a rectangular cell selection, including all rows; mixed alignment has no active button. New rows inherit through the column model rather than copied cell attributes. Pasted cells use destination column alignment; table paste that grows the width extends column metadata.

Cells now contain exactly one paragraph with inline text/marks and soft newline nodes. Enter, Shift+Enter, Ctrl/⌘Enter, and native insertParagraph input cannot create paragraph spacing within a cell. Tab navigation is unchanged. Existing multi-paragraph cells flatten on load, preserving empty lines and marks; non-table HTML flattens before schema parsing, and cell HTML has its own inline parser. This ordering matters: the single-paragraph schema otherwise fits consecutive pasted paragraphs into neighboring cells before a post-parse transform can join them. Rich table paste and tab-delimited spreadsheet ranges still distribute across cells.

Scoped checks passed for divider preview/commit/undo, adjacent-only changes, 75%/150% zoom, keyboard resizing, minimum widths, Escape/zoom cancellation, proportional outer resizing, mixed/multi-column alignment, row insertion and column metadata insertion/deletion, Table/Body theme independence and undo, keyboard/native newline input, rich paragraphs/empty lines/marks and table paste, spreadsheet growth, persisted geometry, old draft normalization, select-all deletion/undo, and idle stability after initial paragraph-ID assignment. Desktop and 390px mobile reading remained noneditable; visual/minimap checks passed. Production preview verified a newly inserted table and its newlines, alignment, and resizing. Build/type and whitespace checks passed; no application browser errors or new dependencies, and the existing bundle-size advisory remains.

Redeployed the demo. Public HTML/JS/CSS match the production build and return no-store with DYNAMIC/BYPASS. Live browser checks confirmed Table style routing, hidden normal class choice, and persisted column width/alignment and soft newlines after reload.

## 2026-09-10 — Favicon

The favicon echoes the existing header mark: a dark-green, slanted lowercase m on a pale-sage tile with an asymmetric lower-left corner. SVG paths keep the mark independent of installed fonts; an ICO fallback includes 16px, 32px, and 48px sizes. The header branding is unchanged. Build/type and whitespace checks passed, the small raster sizes were visually inspected, and the demo was redeployed. Live browser icon links and the SVG payload were verified with no-store/BYPASS headers.

## 2026-09-10 — Inspector hierarchy and header reset

The selection inspector heading now names the item: Main text, Space, Floating text, Floating image, or Floating table. Item type no longer appears in the toolbar; image-opening feedback lives on the image buttons. Selection styles is now Edit style and is the final inspector section, after object/table/image/space controls. Class-specific labels and theme routing are unchanged. Removed the entire Only on this device sidebar section. Reset to example is a compact top-bar action in editing mode only, retaining the existing destructive confirmation and history/selection reset; reading/mobile remain nonediting.

Build/type and whitespace checks passed. Scoped production-preview checks covered all five item headings, final section order, disabled image style actions, Table style routing, View settings, reset cancellation/acceptance and cleared undo history, desktop reading, and header fit from 320px to 1440px. No browser errors observed. Redeployed and verified live inspector/header state and matching HTML/JS/CSS with no-store and DYNAMIC/BYPASS headers. The existing bundle-size advisory remains.

## 2026-09-10 — Drawing system design discussion (not implemented)

User wants a small draw.io-inspired primitive set, with drawing objects mixed directly into the note rather than a separate diagram container. User requires a grid while a drawing object is selected or dragged, visible only in space segments, and confirmed snapping with Alt as a temporary opt-out. Drawing itself is not restricted to spaces.

Proposed details, not yet individually confirmed: rectangle (square/rounded), ellipse, diamond, and straight/elbow connectors with optional endpoint arrows; reuse floating text. Connectors attach to shapes and potentially existing floating text/images/tables. A subtle 16-document-pixel dot grid would appear in all explicit spaces behind content, originate at page-left and each space's top, and remain visible during creation/resizing/endpoint dragging. No grid in reserved text-segment room, reading mode, or minimap. Snap inside spaces, move freely outside; shape attachment takes precedence over grid snapping. Objects created in a space should anchor to it. Semantic drawing appearance classes and remaining interaction details still need discussion. This is design only; no application changes or deployment.

User subsequently excluded Diamond from the first version. The initial drawing palette is Rectangle, Ellipse, and Connector, reusing existing floating text for standalone annotations. This supersedes the proposed diamond above.

User confirmed built-in labels: shapes own their labels, edited by double-click, centered by default, and styled through shared semantic text styles. Moving/deleting a shape carries its label. Connectors support one optional midpoint label. Standalone annotations still use floating text. Label sizing/overflow and the specific theme classes remain to be decided.

User decided labels do not automatically wrap, resize shapes, or otherwise adjust to fit: text may extend outside the shape. Labels have no independently selectable/draggable border or floating-object geometry; users can use separate floating text when needed. Each built-in label is one rich-text paragraph with the fixed paragraph-level semantic class Label (`label`), rather than a selectable paragraph class. This supersedes the proposed wrap/content-driven shape growth (which was not accepted). Shape labels remain centered and connector labels remain at the midpoint; label text remains editable, distinct from selecting a separate label object. Manual line-break behavior has not yet been decided.

User confirmed label keyboard behavior: Enter finishes editing; Shift+Enter inserts an explicit soft line break within the single Label paragraph. No automatic wrapping. Label text supports normal text selection while editing and the existing inline semantic classes; Label typography is configured in the global theme.

User requires per-object drawing colors, superseding the proposed shared Shape/Connector appearance styles or semantic drawing appearance classes. Shape fill defaults to None; shape and connector strokes default to the theme Defaults font color. Individual shapes can choose fill/stroke colors and individual connectors can choose stroke colors. Labels retain the editable Label theme class, with presets identical to Body (not a live dependency on Body overrides). Default strokes should resolve the current Defaults font color rather than snapshotting it; explicit object colors remain fixed. Stroke widths and other appearance controls remain undecided. Design only, not implemented.

User requires dashed strokes in the first version, at least for connectors. Proposed minimal control is per-object Solid/Dashed with Solid as default, a preset dash pattern scaled with stroke width, and solid arrowheads. Extending the same control to shape outlines is proposed, not yet confirmed. Per-object stroke width defaulting to 1 document pixel was proposed previously but has not been explicitly settled.

User accepted Solid/Dashed for rectangle and ellipse outlines as well as connectors, with Solid default, one width-scaled dash pattern, solid arrowheads, and no dotted/custom dash patterns initially.

User accepted the remaining recommendations with refinements: reuse the existing floating-object anchor mechanism for drawings, rather than introducing a separate segment anchoring policy. Existing placement uses document-space x, a nearest preceding measured block anchor (or document-top fallback), and nonnegative anchor-relative y, recalculated on committed movement. Recommended connector behavior is attachment to shapes/text/images/tables, not individual main-text words; automatic outline or four-side pinned attachment; straight/simple elbow routes with a draggable middle segment and no obstacle avoidance; deleting a target detaches its endpoint at its last position. Drawings overlap without repelling text initially. Basic front/back ordering and Shift-click multi-selection are included; no layers/persistent groups. Proposed 16px dot grid and editable per-object stroke width defaulting to 1px were included in the accepted recommendations.

Transparent shape interiors are click-through; filled shapes can be moved by dragging their interiors as well as their outlines. Images also gain interior dragging, superseding the current border-only image movement. Text/table interiors remain for editing. Shape labels remain double-click editable with no separate object selection or border.

User requires rectangular marquee selection for all floating objects (text, images, tables, shapes, connectors), initiated by dragging inside space segments. User broadened grid visibility to selection of any floating object, not only drawings; grid remains limited to explicit space segments. Marquee hit rules/modifiers and whether grid snapping also extends to existing floating kinds were not explicitly discussed yet. These are design decisions only; no application implementation or deployment yet.

User accepted the subsequent unified selection proposal: marquee starts on unoccupied space background (object movement and segment-boundary resizing take precedence), can extend across segments, selects intersecting visible object geometry (actual connector paths, not their bounding boxes), replaces selection by default and adds with Shift. Labels are not separate selection targets. Moving a selected object's move surface moves the selection together. Grid also shows during marquee selection; snapping extends to existing text/image/table objects, with Alt bypass and whole-selection snapping preserving relative positions. The marquee itself is unsnapped.

User clarified there is no separate "drawing" concept, mode, subsystem, or category: add rectangle (including square), ellipse, and line as floating-object kinds alongside text/image/table. Connections, geometric resizing, and labels are type-specific capabilities within a shared floating-object design and implementation. "Connector" describes a line's connection behavior, not a separate object kind. Reuse shared anchoring, selection/marquee, movement, grid/snapping, ordering, history, persistence, and view integration rather than building a parallel editor. This supersedes earlier drawing-specific terminology and architectural proposals.

Images also have optional built-in labels, centered beneath the image. Reuse the same single-paragraph Label-class rich text, inline classes, explicit soft breaks, no automatic wrapping/fitting, no independent label border/selection/dragging, and owner lifecycle as shape/line labels. Image labels are visible content, distinct from the existing accessible alt description. Label positioning does not change image dimensions/aspect ratio. Still design only; no implementation or deployment.

User reopened label design: replace built-in owner content with a dedicated Label floating-object kind, using special snapping/attachment interactions analogous to line connections. Labels remain one rich-text paragraph with fixed Label class. Requested attachment positions are center, top-inside, top-outside, bottom-inside, and bottom-outside. Double-clicking a relevant target object selects an already attached label (any deterministic choice, e.g. the top one, when multiple exist), or creates an attached label if none exists. Multiple attached labels are allowed. This supersedes built-in-label ownership and the exclusion of labels from independent floating-object selection. No automatic wrapping/fitting remains the current text policy. Exact drag/detach behavior, deletion policy, attachment geometry/gaps, relevant target kinds, line-label attachment positions, and editing activation remain to be settled. Still design only.

User requested Left/Right/Above/Below attachment positions around a line's path midpoint, superseding the proposed midpoint-only restriction. Proposed geometry: midpoint measured halfway along path length (including elbow segments), page-relative directions with upright text, label edge separated from midpoint by a shared gap and centered on the other axis. Retaining Center as an additional position and using Above as the default are recommendations, not yet confirmed. No automatic collision avoidance is proposed.

## 2026-09-10 — Unified floating primitives implementation

Implemented the first unified floating-object slice. V0 now supports rectangle, ellipse, line, and standalone Label objects alongside text, image, and table. There is no separate drawing subsystem or mode. Shared canvas behavior includes multi-selection, Shift-add, rectangular marquee from unoccupied space segments, 16px space-only grid, grid snapping with Alt bypass, group movement, front/back and duplicate/delete actions, document-space anchors, zoom, minimap stripping, history, and persistence.

Rectangles/ellipses support per-object fill (None by default), stroke (null follows current Defaults font color), stroke width, Solid/Dashed outline, and rounded rectangles; Shift creation/resizing constrains squares/circles. Lines support straight/simple elbow routes, per-object stroke settings, arrows at either end, draggable endpoints, four-side/automatic object connections, and line bend movement. Transparent rectangle/ellipse interiors are click-through; filled shapes and image interiors move. Text/table interiors retain editing. Connected endpoints and labels detach in place when their target is deleted.

Labels are separate one-paragraph rich-text floating objects with fixed Label theme semantics and Body-equivalent initial presets. They never auto-wrap or resize targets, support inline classes and Enter/Shift+Enter behavior, and attach/snap to center/top-inside/top-outside/bottom-inside/bottom-outside for boxes/images/text/tables or center/left/right/above/below at line path midpoints. Double-clicking an attachable object selects or creates a label; multiple labels are allowed. Attached labels follow target geometry, and Alt detaches during free movement. Image labels are therefore ordinary attached Label objects, separate from alt descriptions.

The example draft now includes rectangle, ellipse, connected dashed line, shape labels, and an image label. README model/boundary documentation was updated. Build/type checks pass. Scoped Chromium checks passed for shape creation/snapping, grid visibility, label editing/paste/inline marks, line connections/elbow routes/directional labels, group movement, Alt bypass, atomic undo, target deletion detachment, image interior movement, image label centering, filled-shape movement, dashed outlines, minimap control removal, and zoomed interaction geometry. No deployment yet in this task.

The implementation was deployed after build and smoke checks. Production HTML/JS/CSS are live at https://mote.ylxdzsw.com with no-store/DYNAMIC headers; deployed assets match the production build. Existing browser-local drafts intentionally remain, so an existing draft can show its prior floating objects until Reset to example is used.

## 2026-09-10 — Quiet space cursor, manipulation-only grid, and horizontal bounds

User requested a normal arrow cursor over space backgrounds, grid visibility only while dragging/resizing floating objects, and prevention of movement beyond the document's right edge. Space backgrounds now use default cursor (crosshair while a creation tool is active). Object selection, text editing, hover, and marquee selection no longer reveal the grid. Actual creation/move/resize previews and active table-column dragging reveal it in explicit spaces only, clearing on release/cancellation. This supersedes selection/marquee-triggered grid visibility.

Shared translations now clamp the selection to the page's inner horizontal bounds after snapping, including keyboard movement and duplication; Alt bypasses snapping but not containment. Attached labels and fully moving connected lines contribute to group extents while preserving relative placement. Existing oversized groups can move toward the page but not farther outward. Outer-width/shape resizing, creation, free line endpoints, and elbow bends are also horizontally bounded. Fractional computed border widths keep the right edge accurate at non-integer zoom. This constrains interactions, not global relayout: narrowing a document or growing unwrapped label text still does not rewrite stored geometry or wrap/clip labels.

Production build/type and whitespace checks passed. Scoped Chromium checks covered normal space cursor, hidden grid for selected/focused text and marquee, grid during movement/resize and clearing on release, Alt right-edge movement, keyboard bounds, shape resizing, group-relative placement, attached-label bounds, and image movement/resizing at 150% zoom. Essential geometry checks covered attached selection extents and returning an existing overflowing object inside the page. No application browser errors observed.

Redeployed; public HTML/JS/CSS hashes match the build and retain no-store/BYPASS. Live browser verification confirms the default space cursor and hidden idle grid.

## 2026-09-11 — Toolbar menus and responsive shortcuts

User chose three permanent menus (current paragraph class, current inline class, and Plus for insertion), with additional shortcuts exposed at wider widths. Complete menus remain independent of shortcut sets so more classes and object kinds need not crowd the bar. Paragraph shortcuts are Heading/List/Code; no Body shortcut. Clicking a uniformly active paragraph shortcut returns to Body. Inline shortcuts are Bold/Primary/Secondary/Term, with Bold the highest priority; clicking a uniformly active inline shortcut returns to Plain. Other class choices replace the current class. Mixed selections apply the requested class throughout, including rectangular cell selections; dropdown choices always apply explicitly, never toggle. Existing List/Code keyboard interactions and single-inline-class semantics remain.

Insertion shortcuts are Text box/Image/Rectangle/Line. Icons identify conventional actions, while Primary/Secondary/Term use short labels with theme-derived samples. Menus show labeled icons/samples and support keyboard navigation, type-ahead, Escape, and outside dismissal while preserving editor selections. Plus and the matching shortcut indicate an armed drawing tool. List indentation commands now live in the paragraph menu, keeping positions stable across selection changes. Table/Label show their fixed paragraph class and disable paragraph shortcuts; Code disables inline controls. The 37px toolbar keeps fixed shortcut tiers: menus/history only below 850px, then Bold, then Heading/List/Text box/Image at 1050px, then all chosen shortcuts at 1250px. Mobile remains reading-only.

Build/type and whitespace checks passed. Scoped browser checks covered paragraph/inline toggles, mixed selections including rectangular table ranges, explicit menu application, selection preservation, keyboard menu navigation, undo/redo, fixed Table/Label classes, Code restrictions, floating text/label editing, drawing-tool indicators, and text/table/space insertion. Visual checks covered wide and compact menus; 1440/1100/900/768px tiers fit without horizontal overflow, and 390px remains reading-only. Live smoke checks passed with no application browser errors. Deployed HTML/JS/CSS match the final build and return no-store with DYNAMIC/BYPASS. No dependencies or regression suite were added; the existing bundle-size advisory remains.

## 2026-09-11 — Reserved segment edit flicker

Reproduced a one-frame jump during class changes and line splits/joins in segments with saved minimums. Replacing the padded paragraph temporarily removes its decoration; delayed measurement then published collapsed segment boundaries and floating anchors. Measurements now settle through coalesced microtasks before paint, and boundaries publish only after padding and minimum normalization settle. Floating repulsion still measures intermediate content, but anchors from invalidated segment-decoration state never reach the floating-object render. The temporary editor-container floor now protects scroll extent when any segment has reserved padding, superseding the final-segment-only protection; it derives from settled block geometry and clears when no padding remains. No persistent layout or history semantics changed.

Build/type and whitespace checks passed. Frame-by-frame checks covered leading/middle/final segments, paragraph splits/joins, soft line breaks, Heading/Code/List/Bold toggles, floating anchor positions, 75%/150% zoom, earlier-segment edits while scrolled to the bottom, minimum release on growth with undo/redo, boundary resize/Home reset, and active text repulsion. No idle transactions or application browser errors. Deployed HTML/JS/CSS match the build with no-store/DYNAMIC/BYPASS; live Heading/Body/Bold/Enter/Backspace checks kept segment and floating positions stable in every sampled frame.

## 2026-09-11 — Immediate space selection with marquee support

The canvas marquee capture handler had bypassed the editor's immediate spacer handler: pointer-down cleared the active editor, temporarily showing Main text and disabling insertion, while spacer selection waited until release. The canvas now selects and focuses the spacer on pointer-down, keeping that selection through a click or sub-threshold movement. Only crossing the existing 3-screen-pixel marquee threshold clears the active editor for object selection. Shift-additive object selection retains its existing behavior. This restores the atomic space-selection decision without delaying or masking inspector/toolbar updates.

Build/type and whitespace checks passed. Scoped Chromium checks covered held/repeated clicks, text/object-to-space transitions, text margins, sub-threshold movement, marquee takeover, 150% zoom, and subsequent text-box insertion. No application browser errors. Redeployed and verified live pointer-down selects Space with text/image insertion enabled; public HTML/JS match the build and JS retains no-store/BYPASS. The existing bundle-size advisory remains.


## 2026-09-11 — Borderless gaps, no text reservations, global grid

User chose the explicit-space-only model after comparing flowing notes against PPT-like reserved text areas. Remove text-segment minimum heights rather than retaining two meanings for blank room. Spaces are unselectable and have no border controls or inspector. Alt-drag at a paragraph break creates a gap; Alt-drag inside a gap resizes it with its top fixed. The target extends through the first text line below a gap so pulling that line up can close an otherwise invisible space. Objects keep their own interactions and Alt snapping bypass; normal text dragging remains text selection.

Use a fixed 12-document-pixel removal threshold, evaluated on drag release, not relative to line height or during theme changes. A short insertion cue appears in the margin only while Alt-targeting a paragraph break; a temporary height/removal hint accompanies dragging. Releasing Alt after pointer-down does not interrupt the gesture. Preview geometry is transient; release commits one shared undo step, including merges. Escape, pointer cancellation/lost capture, window blur, zoom/mode changes, and document changes cancel. Theme paragraph margins retain their normal collapsed spacing when separated by an explicit gap.

Adjacent spaces automatically merge on edits and load, retaining combined height and transferring floating-object/line-endpoint anchors with accumulated offsets. Removing a space preserves its objects and does not impose an object-based height floor; overlap is allowed. V0 no longer interprets old minimum-height attributes and does not migrate old text reservations. Unparseable saved drafts offer a confirmed reset to the example, following the updated project guidance.

User also requested marquee selection from the left/right gutters between the document and minimap/inspector. These share the space-background gesture, Shift-add behavior, object intersection rules, and unsnapped marquee. Actual minimap/bookmark/menu controls retain their own actions.

The grid is now global: one 16px lattice rooted at the page’s top-left, visible across the page only during floating-object dragging/resizing (including creation and table columns). Snapping uses that same origin in both text and space areas, with Alt bypass. Resizing or merging gaps cannot shift it. Ordinary selection, hover, text editing, marquee, reading mode, and minimap stay grid-free. This supersedes all prior space-local grid visibility/origin/snapping rules.

Production build/type and whitespace checks passed. Scoped Chromium checks covered gap insertion and first-line closure, the 12px release threshold, transient preview/cancellation, Alt release mid-gesture, paragraph-margin continuity, click-without-spacer-selection, 150% resizing and zoom cancellation, consecutive-space/load/deleted-text merging, object and line-endpoint offset preservation, atomic undo/redo, both gutter marquees, and page-wide grid/snapping in text areas. Desktop and 390px mobile reading remained noneditable and grid/control-free; idle layout had no repeating transactions. Malformed-draft confirmation was checked for both declining and accepting reset. No application browser errors, new dependencies, or regression suite. The existing bundle-size advisory remains.

Redeployed and verified matching public HTML/JS/CSS with no-store and DYNAMIC/BYPASS responses. Live real-pointer checks passed for space creation/closure/resizing/cancellation, merging and anchor offsets, undo/redo, both gutter marquees, global grid/snapping, and 150% sizing. Temporary dev/preview processes started for this task were stopped.

## 2026-09-14 — Local-draft ownership and focused recovery

User approved a small draft-reliability milestone, explicitly excluding a validation/compatibility layer. Keep only the current V0 identity check and editor-schema initialization; removed historical theme/class upgrades, Emphasis renaming, and saved multi-paragraph table conversion. Current editing normalization (including adjacent-space merging and anchor transfer) remains. There is no whole-document validator, migration registry, repair policy, or compatibility promise. Editor parsing failures offer the existing confirmed reset only in the owning tab; declining does not write the saved draft. Arbitrary malformed envelope/geometry fields are not exhaustively checked.

Desktop requests the exclusive `mote-local-draft` Web Lock before loading for editing. A competing tab displays a read-only snapshot and a Try editing action. Each attempt is non-waiting; successful acquisition reloads IndexedDB and remounts the editing session with fresh undo history before enabling editing or autosave. There is no live synchronization, merging, forced takeover, or automatic handoff. Ownership remains held for the tab's lifetime, including reading-mode switches and load retries; closing/reloading releases it. Initial mobile reading does not acquire a lock or save the example/normalization. Widening a reader to desktop exposes Try editing; an existing owner retains ownership when narrowed to mobile. All autosaves, reset writes, retries, and normalization writes require writer access.

User explicitly requires a silent fallback when Web Locks is unavailable: normal editing and autosave remain functional in every tab, accepting last-writer-wins concurrent editing. No alternative locking protocol or warning is introduced for that case.

Storage-access failures now show Retry without a reset prompt. Failed database-open promises are cleared so retry can reopen the database in the same page. Reads and writes resolve on transaction completion and reject on errors/aborts. Failed saves retain the latest in-memory draft and its unload warning; Retry saves that current state without a history entry. Readers show Reading local draft, never a misleading saved/saving indicator. Ctrl/⌘S remains suppressed during loading and failure screens as well as editing/reading.

Essential checks passed in development, production preview, and isolated live browser contexts: two-tab exclusion (including reset/autosave), reading-mode ownership retention, fresh handoff/history, reload, transient database-open failure and same-page retry, aborted reads/writes, quota failure and latest-draft retry, unload warnings, parsing-reset decline/acceptance, blocked invalid-draft readers, mobile reading without writes/locks, and the no-Web-Locks concurrent-editing fallback. Additional production checks confirmed that reader-side space normalization never writes, while ownership acquisition saves merged heights and floating-object/line-endpoint anchor offsets without adding undo history. Blocked layouts fit 1440/768/390/320px. No application errors in the final checks; no dependencies or regression framework added. Non-waiting lock requests are deferred until discarded React mounts have cleaned up, and load retries retain their existing lock rather than racing its asynchronous release.

Build/type and whitespace checks passed. Deployed HTML/JS/CSS match the production build and retain no-store with DYNAMIC/BYPASS. The existing bundle-size advisory remains (about 654 kB JS / 203 kB gzip).

## 2026-09-15 — Local document files and shared offline reader

User approved three formats: native-resolution full-length PNG, standalone offline HTML, and `.mote`. PDF is explicitly deferred. The visual page keeps its configured width and crops horizontal overflow rather than expanding or reflowing; this applies to shared reading mode and its minimap as well as exports. PNG uses one bitmap pixel per document CSS pixel, ignores viewport zoom/display density, includes normal document height/background/spaces/floating content, and removes surrounding chrome. Fractional bitmap extents round upward. Initial PNG limits are 32,767px tall and 32 million pixels with runtime canvas/HTML-rasterization checks; unsupported/oversized exports fail explicitly rather than shrink or truncate.

HTML reuses the full DocumentCanvas reader and its schemas, theme, anchor/repulsion measurement, floating layout, zoom, minimap, and bookmarks. ReadDocument supplies in-memory normalization/history plumbing for both the offline reader and native-scale PNG capture. A separate minimal entry supplies only reader controls and nonpersistent shared View settings; it does not initialize local drafts, ownership locks, autosave, or storage. The Vite build bundles this entry as an inline production IIFE/CSS with embedded document/images; exported files work directly from disk without network access. Existing system fonts remain, so cross-device typography can differ and layout recalculates. There is no export-specific renderer or duplicated read-mode implementation.

`.mote` is gzip-compressed UTF-8 JSON of MoteDocument itself, not `.json`, a ZIP archive, or an outer base64 encoding. V0 remains the only current format with no backward-compatibility promise or migrations. Export captures current in-memory content even when browser saving fails. Session state is excluded. Import has bounded gzip/UTF-8 decoding (128 MiB compressed/decompressed limits), external-file shape/geometry/asset safety checks, and shares current-schema initialization with local draft loading; saved-draft recovery semantics are unchanged. Confirmed import replaces the single local draft and remounts a fresh editing session even for the same document ID. Failed/canceled imports never replace the draft or offer reset-to-example. Only `.mote` is supported for import; HTML/PNG are publication formats. Export is available in reading/mobile/blocked tabs; import requires desktop editing ownership.

Implemented the File dialog and lazy-loaded export paths without dependencies. Essential development checks passed for round-trips, empty alt text, canceled/invalid/oversized import, same-ID history reset, unsaved-content export during quota failure and save retry, blocked-reader export without writes, native/150% byte-identical PNGs, DPR2/theme/margins/repulsion, full floating composition, long-image errors and cleanup, matching app/offline native geometry, safe embedded script-like text, local-file operation with storage trapped/network disabled, heading navigation and horizontal zoom, and 390/320px mobile layouts. Build/type/whitespace checks pass; production deployment follows final smoke checks.

Production preview and isolated live UI checks passed for all three downloads, confirmed same-ID import/history reset, byte-identical mobile-DPR2/native PNGs, and exported HTML opened from disk on mobile with storage traps and networking disabled. The deployed HTML and every JS/CSS chunk match the production build and return no-store with DYNAMIC/BYPASS. No application errors were observed. The existing bundle-size advisory remains; the roughly 628 kB reader-export chunk is lazy-loaded, not part of initial startup. Temporary dev/preview processes and browser sessions for this task were stopped.

## 2026-09-15 — Native Save As and export filenames

User requested a native Save As chooser where possible, or filename entry when downloading. Export now feature-detects showSaveFilePicker and invokes it directly in the click gesture, before lazy imports/rendering can consume transient activation. A session-local Filename field supplies the suggested native name and the fallback download name; known format extensions are replaced with the selected extension. The native dialog can choose a different name/location. Canceled pickers do not trigger generation or fallback downloads. Writable streams are opened only after generation, closed before success is announced, and aborted on write/close failures. Unsupported browsers retain ordinary downloads, whose location prompts are controlled by browser preferences. No document-format, autosave, or history changes.

Production-preview checks passed for all three native-save contracts using trusted clicks, injected picker selection, and real browser FileSystem writable handles; all three fallback names including Unicode/extensions; cancellation without download; failed-write abort/error handling; invalid names; retained modal filename; and the 320px mobile layout. Type/build/whitespace checks passed. Native OS chooser interaction itself is outside the headless checks.

Redeployed and repeated the same scoped checks on the live build without application errors. Public HTML and all JS/CSS match the build and retain no-store with DYNAMIC/BYPASS. Task-owned preview and browser sessions were stopped.

## 2026-09-15 — KaTeX and HTMLWidget floating objects

User approved two regular floating-object kinds, sharing anchors, selection/marquee, movement/grid, bounds, ordering, labels/line connections, overlap/repel, document history, and V0 persistence. KaTeX stores LaTeX source and renders display mode only, centered within a user-width box with natural height. No auto wrapping/shrinking or delimiter wrappers; explicit multiline math is supported. Syntax errors retain source and render locally. Math is an object-specific global size/color style inherited from Defaults, not an editable paragraph class; px/em semantics and resets follow existing theme behavior. Math typefaces belong to KaTeX, with trust disabled for unsafe HTML commands. Pinned KaTeX 0.18.7 and its WOFF2 fonts are embedded in app/reader output so formula PNG and standalone HTML do not depend on network fonts. Missing Math overrides mean inheritance, not a migration or draft rewrite.

After discussing JSON-only ECharts and JavaScript sandbox engines, user prioritized accidental namespace conflicts rather than hostile-code protection. HTMLWidget runs arbitrary author HTML/CSS/JS in one iframe per object, isolating DOM IDs, styles, global variables, and library instances. Basic sandbox permits scripts without same-origin privileges or top navigation; no QuickJS/SES, chart-only schema, trust dialog, or CPU/memory quota. Source saves immediately in a larger editor, but Run / Restart recreates the iframe explicitly. Runtime interaction state is not document history or saved state. Reading/mobile interiors are directly interactive; editing uses Interact / Exit interaction. Width/height are explicit and resize the widget viewport, while document zoom scales the object. Floating DOM order is stable by ID and CSS z-index follows saved stacking order, because ordinary DOM reordering would reload iframe browsing contexts; movement/resizing/reordering/mode switches therefore retain runtime state.

User explicitly deferred automatic HTML snapshots and requires an uploaded screenshot. Creation requires an image first; replacement is manual, uses existing image validation/10MB limit, and does not verify correspondence to source or runtime. Screenshot fills the saved rectangle and supplies PNG/minimap content. The minimap removes live iframe subtrees before connecting its cloned DOM, while PNG uses a screenshot-only shared reader that never starts widget scripts. `.mote` stores source, geometry, alt text, and screenshot and validates their shapes/image safety without inspecting HTML code. Exported HTML runs live sandboxed widgets. User requires external/CDN references to remain as-is: no inspection, rewriting, dependency bundling, or offline guarantee for online resources. Removed the exported reader's restrictive inherited CSP because it prevented these resources; document serialization still safely escapes the outer HTML payload. ECharts works as ordinary widget source with an author-provided CDN script; it is not a separate object kind or bundled dependency.

Essential development and production-preview checks passed for live/error/multiline formulas, Math-only controls and embedded-font PNG rendering, widget creation from screenshots, source-versus-Run behavior, isolation, stable resizing/reordering/mode switches, shared keyboard/corner geometry and undo, narrow-page insertion, reload persistence, native roundtrip and missing-screenshot rejection, static minimap/PNG without extra iframe execution, standalone file operation with networking disabled, and 390/320px reading. A CDN-loaded ECharts 6 figure with a function-valued formatter ran successfully; exported HTML retained its URL unchanged. All three File-dialog exports passed. Scoped screenshots were visually reviewed and a readonly correctness review found no blocking issues. Type/build/whitespace checks pass. The bundle-size advisory remains; embedded KaTeX/font support brings initial JS to about 1.30 MB / 550 kB gzip and the lazy reader-export chunk to about 1.27 MB / 539 kB gzip. No regression suite was added. Deployment and live smoke checks follow.

Deployed the final build. Live checks passed for native import of both kinds, formula source and relative Math style changes, explicit widget Run, real-pointer interaction, all three File-dialog exports, and mobile read-only composition with interactive widget interiors. The live-exported HTML reopened from disk without application errors. Production HTML and every JS/CSS chunk match the local build and return no-store with DYNAMIC/BYPASS. Task-owned development/preview processes and the isolated browser session are being stopped; no screenshot backend or other persistent service was introduced.

## 2026-09-15 — Resize spaces at the drag origin

Alt-resizing an existing space now inserts/removes room at the pointer's starting document position rather than only at the gap's bottom. Floating-object origins and free line endpoints at or below the start shift by the gap-height delta; those above stay fixed. Capture the cutoff and preceding anchor positions on pointer-down, so affected objects anchored to earlier paragraphs or document-top also move, while later anchors continue to follow text flow without a double shift. Attached labels and connected endpoints retain their target-derived geometry. Upward movement permits overlap and retains nonnegative anchor offsets. Targeting the first text line below a gap uses the gap bottom as the cutoff.

Floating previews share the transient space-layout state; commit sends the same shift through main-content replacement so geometry and spacer edits remain one undo step. Complete gap removal transfers its remaining offsets to the preceding measured anchor without an unrelated position jump. The existing removal threshold, cancellation behavior, and space normalization remain unchanged.

Type/build/whitespace checks and essential anchor/free-endpoint invariants passed. Trusted Chromium pointer checks covered downward/upward origin-relative shifts, unchanged objects above the start, following text/later anchors, attached labels and connected lines, transient preview versus commit, atomic undo/redo, Escape, 150% zoom, and below-threshold removal with preserved above-origin objects and undo. Redeployed and verified live down/up/undo, matching HTML and all JS/CSS assets, no-store headers, and no application browser errors. The existing bundle-size advisory remains; no dependencies or regression suite added.

## 2026-09-15 — Formula and chart examples

The default document now includes a six-row KaTeX sampler and two ordinary HTML widgets, each in its own anchored space. The sampler covers aligned equations, fractions/roots, scripts, Greek letters, sums/binomial coefficients, integrals/derivatives, matrices/underbraces, cases, and vector notation. A week of attention demonstrates an `<attention-chart>` custom element with shadow DOM, measure switching, legend filtering, tooltips, bar-click details, and a composed `attention-select` event. Ideas in motion demonstrates repeating deterministic bar rankings with animated lengths, sorting, and numeric labels, plus Pause/Play and Next pass. Reduced-motion preferences disable transitions and start the ranking paused.

Both chart sources use pinned ECharts 6.0.0 from jsDelivr, not a bundled application dependency. Source HTML and manually captured 690 × 400 PNG previews live in `src/document/examples/`; previews are embedded in the document for minimap/PNG output, with the ranking preview showing paused pass 4. No automatic screenshot feature was added. Existing browser-local drafts stay unchanged; confirmed Reset to example loads the new 38-block/14-object composition.

Type/build/whitespace checks passed. Scoped browser checks covered formula layout without errors, two-way measure/bar-click details and custom events, animation tweening/stepping, reduced motion, desktop editing Interact and reading controls, 390px reading layout, embedded minimap previews without live iframes, and all three document exports. PNG composition was visually reviewed; native export retained all three embeds and embedded screenshots; standalone HTML retained CDN references and live widgets. Redeployed and verified live formula/widgets and custom-element measure switching, matching HTML/main JS/CSS, and no-store with DYNAMIC/BYPASS. No application browser errors; the existing bundle-size advisory remains. Task-owned preview and browser sessions were stopped.

## 2026-09-16 — Keep only the 48px margin preset

User found margin rulers and margin snapping too complex and requested their removal. Reverted the entire margin-guide commit, then restored only the 48px left/right preset for new/example documents. Existing saved margins remain unchanged. Floating interactions return to their prior grid/attachment snapping without margin rulers or margin-specific snapping.

## 2026-09-16 — GitHub continuous integration

CI runs locked dependency installation and the existing combined TypeScript/production build on Ubuntu with Node.js 22 and 24 for pushes, pull requests, and manual dispatches. This includes the offline reader bundle without adding a regression framework. Actions are pinned to release commit SHAs, repository permissions are read-only, and superseded runs are canceled. Only npm downloads are cached; this is unrelated to the demo's no-store HTTP policy. CI deliberately has no deployment credentials or automatic deployment; the existing demo deployment remains separate. README links the live workflow status and describes its scope.


## 2026-09-16 — Direct creation, quiet undoable actions, table picker, image drops

Removed floating deletion confirmation; reset/import/recovery retain confirmation because they replace the draft and clear history. Space no longer appears in the object menu; Alt-drag remains its creation/resizing interaction.

Direct insertion now uses a transient shared-canvas measurement before committing one history step. It starts at the retained main-text caret (or an image drop's document-space point), bounds width/placement, and staggers by 24px when either object's measured rectangle fully contains the other. Partial overlap is allowed; there is no ongoing collision-avoidance layout. Insertion remains enabled with floating selections. Click-created default shapes/free lines and newly finished unattached labels receive the same placement policy; explicit dragging and intentional attachments keep their positions. Duplication staggers the group without breaking internal attachments. Default click-created lines point inward at the page's right edge, and collapsed line gestures leave no invisible line.

Empty/whitespace/default-ignorable-only labels and text boxes disappear when editing/selection ends; blank formulas disappear on deselection. Cleanup joins the preceding edit rather than adding an extra undo step, and opening an editable draft normalizes abandoned empty content without an undo step. History restoration is not a cleanup trigger, and readers never perform this cleanup. Empty tables are retained as usable grids. Connections/labels detach in place when cleanup removes a target, just as with explicit deletion.

The Table toolbar shortcut replaces Image and opens an 8×8 hover/click/arrow-key picker. The ordinary insertion menu keeps a default empty 2×2 table. Images remain available in the menu and can be dropped, singly or in batches, anywhere on the document (including editor interiors). Drops use existing format/10MB validation, measure actual image size, respect zoom/page bounds, and commit a batch atomically; invalid batches insert nothing. Pending image reads are canceled across mode/document/history replacement. Sheet stacking is isolated so floating objects cannot cover toolbar menus.

Type/build/whitespace checks and essential insertion-containment/page-bound invariants passed. Scoped development and production-preview checks covered keyboard/pointer table sizing (including atomic 8×8 undo/redo), default 2×2 menu insertion, repeated text/formula/image/widget insertion and duplication, immediate deletion/undo without prompts, empty/whitespace labels and text cleanup with content-restoring undo, saved zero-width-label cleanup only after acquiring editing ownership, inward click-created lines and collapsed gestures, multi-image drop undo/redo, invalid-file rejection, 150% drop coordinates, and 1440/1100/768/390px layouts. Picker screenshots were visually reviewed. No application browser errors or dependencies were added; the existing bundle-size advisory remains.

Redeployed and verified live picker dimensions, menu changes, deletion/undo, repeated insertion staggering, image drops, and empty-label cleanup. Public HTML and every JS/CSS chunk match the production build and return no-store; HTML reports DYNAMIC. Task-owned browser and dev/preview sessions were stopped.

## 2026-09-16 — Configurable top and bottom margins

All four document margins now default to 48px. Layout exposes independent top/bottom fields (0–2400px, including zero); linking left/right never changes vertical margins. Top spacing precedes main text and therefore moves text-anchored objects through normal reflow, without rewriting floating geometry. Bottom spacing follows whichever extends farther: main text or the lowest floating object's visual bounds, including strokes/line decorations. This supersedes fixed top padding and the earlier no-bottom-margin decision. Shared canvas rendering carries these settings into reading mode, minimaps, PNG, and standalone HTML.

V0 requires all four finite, nonnegative margin values; no legacy-margin migration was added. Invalid saved drafts, including earlier drafts missing top/bottom, use the existing confirmed reset-to-example flow. Native imports validate all four margins.

Build/type/whitespace checks passed. Scoped production-preview checks covered defaults, independent changes, zero margins, linked side edits preserving vertical values, undo/redo, reload persistence, main-text and low-floating-object bottom extents, reading mode, PNG dimensions, native export values, standalone desktop/mobile reading, and the malformed-draft recovery prompt. No application browser errors were observed. Redeployed; live controls show 48px defaults and public HTML/all JS/CSS match the build with no-store. The existing bundle-size advisory remains; no dependencies or regression suite were added.

## 2026-09-16 — Default Auto zoom and dynamic detent

Auto is the first zoom-dropdown option and the default in editing, reading/mobile, and standalone HTML. The closed control still displays the actual rounded percentage; its tooltip identifies Auto without adding a top-bar button. Auto fits the canvas content width, limited by a minimum visible height of 600 document pixels and the existing 300% ceiling. Canvas padding is excluded from available dimensions. ResizeObserver follows viewport/panel/minimap changes, and document-width edits recalculate it. Small fits remain reachable below 25%.

Manual presets, buttons, wheel, pinch, and keyboard increments leave Auto, including an increment that snaps to the same scale. Manual zoom is an absolute value and stays unchanged across resizes and editing/reading switches; this supersedes mode-baseline resets and fit-relative reading zoom. Selecting Auto or Ctrl/⌘0 restores automatic scaling. Reading Fit remains a separate exact one-shot width fit capped at 100%. Zoom remains session-only, not saved geometry.

Incremental zoom has relative 5% detents at both 100% and the currently calculated Auto scale, even outside Auto mode. Overlapping detents choose the nearer multiplicative scale. Wheel accumulation and gesture-relative pinch input let continued input escape either detent; snapping to Auto's value never enables Auto. Exact presets, Fit, and Auto bypass detents. Native PNG capture explicitly starts at 100% independently of viewport fitting.

Type/build/whitespace checks passed. Production-preview checks covered default width fit, ultrawide height limits, panel/document-width/minimap changes, fixed manual scales, Auto re-entry/reset, button/wheel/pinch snapping and escape, the unchanged 100% detent, reading Fit, 390/320px mobile, and standalone HTML Auto/manual resizing. PNG exported at the matching native 800×4066 size. Mobile header screenshot was reviewed; no application browser errors were observed. The existing bundle-size advisory remains; no dependencies or regression suite were added.

Redeployed and verified live default Auto, the dynamic detent without re-entering Auto, fixed manual zoom across viewport changes, and the ultrawide height limit. Public HTML and all JS/CSS match the production build and return no-store (HTML DYNAMIC). Task-owned preview and isolated browser session were stopped.

## 2026-09-16 — Fold minimap heading labels

Heading bookmarks now rest as 10px tabs without visible text; hovering an individual tab expands its existing label, and leaving folds it again. Keyboard focus also reveals the focused label for accessible navigation. Current-heading tint, full accessible names/tooltips, heading jumps, and canvas layout remain unchanged; pointer clicks do not leave labels expanded. This shared CSS also applies to standalone readers.

Build/type/whitespace checks and scoped Chromium checks passed for individual expansion, collapse after navigation, retained current-heading state, and keyboard focus. Redeployed and verified live folding/hover behavior and matching HTML/CSS with no-store headers. The existing bundle-size advisory remains.

## 2026-09-16 — Expand bookmarks together on minimap hover

User clarified that hovering the minimap should expand all heading labels, not individual labels. Hovering anywhere in the minimap pane now reveals them together and keeps them open across the label lane, including gaps between labels. Leaving folds them back into 10px tabs; keyboard-focused labels remain readable. Expanded width caps increase exactly 50%: 60→90px on desktop and 40→60px on small screens, with bookmark lanes increasing to 96/66px. Canvas layout and zoom remain unchanged.

Build/type/whitespace checks passed. Scoped Chromium checks covered group hover, movement into the label lane, collapse after leaving/navigation, keyboard focus, and small-screen fit-mode widths. Redeployed and verified live group expansion/folding and matching HTML/CSS with no-store headers. No browser errors; the existing bundle-size advisory remains.


## 2026-09-16 — Caption-like labels and drag-created transparent text boxes

Label presets now match Caption (0.75em, muted color, 1.25em line height), independently editable rather than dynamically inheriting Caption. This supersedes the Body-equivalent label preset; existing saved theme overrides are not rewritten.

Text box now arms the shared creation tool. Horizontal dragging in either direction determines width, with the existing 120px text-width minimum where page bounds permit; its top stays at the starting point and height follows content. Grid snapping, Alt bypass, zoom, bounds, transient preview, cancellation, and atomic creation history reuse canvas behavior. A click retains the ordinary default-width insertion fallback. New boxes contain one empty Body paragraph and immediately focus their text editor. The conventional empty/whitespace/default-ignorable cleanup on editing/selection completion is unchanged, including content-restoring undo and reader exclusions.

Text objects have independent optional background and borderColor fields; null or omitted means transparent. The inspector chooses each color or clears it with None. Saved borders remain distinct from editing hover/selection outlines. Shared rendering carries the appearance through reading, minimaps, and exports; native import validates the optional colors with the existing safe-color rules. No compatibility migration or dependency was added.

Type/build/whitespace checks passed. Scoped production-preview checks covered empty creation/focus, dragged widths in both directions at 100%/150%, deselection cleanup, text retention, undo after clearing, cancellation, transparent and explicit colors, color resets, reload persistence, native roundtrip/unsafe-color rejection, caption-equivalent label rendering, and read-only appearance. Final editing screenshot reviewed; no application browser errors. Redeployed and verified live insertion/focus/cleanup, matching HTML and all JS/CSS chunks, and no-store headers (HTML DYNAMIC). The existing bundle-size advisory remains. Task-owned preview and browser sessions are stopped after verification.

## 2026-09-16 — English-first Simplified Chinese documents

User narrowed the CJK discussion to everyday mixed English/Simplified Chinese notes, not UI localization or a general typesetting system. Vertical writing and language-driven right alignment/RTL layout are explicit non-goals; existing table column alignment remains independent. Per-passage languages, other CJK typography policies, ruby, advanced punctuation compression, and embedded/portable document fonts are not part of this slice.

Sans now prefers Microsoft YaHei wherever installed, as explicitly requested, followed by PingFang SC and the Simplified Chinese Noto Sans families. Serif retains Georgia for Latin and adds Songti SC (macOS), SimSun (Windows), and the SC Noto Serif families. Monospace retains system Latin monospace with Sarasa Mono SC / Noto Sans Mono CJK SC and YaHei fallbacks; exact mixed-script cell alignment is not promised. All are local font choices, with no new font download or redistribution. The local verification browser actually rendered body text with Microsoft YaHei; no SC serif font is installed on this host, so its Chinese serif text uses the system fallback.

Optional document language is en (default) or zh-Hans, selected in Layout and applied to the shared sheet, not the English shell. Optional theme.autospace defaults true and has one Defaults checkbox. CSS adds visual inter-script spacing without mutating text, skips Code, and falls back naturally on browsers without support. Prose explicitly sets normal word-breaking and strict CJK line-breaking on the editor itself: Tiptap injects a legacy after-white-space rule that otherwise defeats inherited strict breaking. Emergency long-token wrapping and nonwrapping labels remain unchanged. Title/Heading presets now inherit zero default tracking instead of imposing Latin-oriented negative spacing; saved overrides are not rewritten. Theme samples include both languages. Both optional settings validate and persist through native files and history, without a schema migration or automatic script tagging.

Custom history now groups ProseMirror composition IDs per editor without a 750ms timeout, and selection-only composition transactions do not split the group. History navigation does not restore snapshots into active compositions. Global save/undo/zoom/Escape/object and resize key handlers yield to isComposing or process-key 229; table paragraph-input and editor history-input handling also defer during composition. Upstream ProseMirror remains responsible for editor-level candidate-confirmation Enter and platform-specific composition behavior.

Build/type/whitespace checks passed. Scoped Chromium checks covered actual installed YaHei selection; language default/change/undo/redo/reload and minimap inheritance; visual autospace on/off with unchanged text and a literal Code exception; native roundtrip and invalid-setting rejection; and browser-injected composing updates separated by >750ms with atomic undo/redo in main text, floating text, tables, and labels. Composing Escape/Enter/zoom/undo and table paragraph-input did not trigger document commands. Production PNG dimensions matched the native reader (800×676), standalone offline HTML exactly matched paragraph text/styles/heights and page geometry, and 390/320px offline reading stayed noneditable without shell overflow. Editing/PNG/mobile screenshots were reviewed. No dependencies or regression suite were added; the existing bundle-size advisory remains. This is not native Windows/macOS IME candidate-window testing or Safari/Firefox certification.

Redeployed and verified live English/Simplified Chinese selection, unchanged English UI, minimap language inheritance, Microsoft YaHei-first styling, strict breaking, and spacing on/off. Public HTML and all JS/CSS chunks match the production build and return no-store (HTML DYNAMIC, assets BYPASS). Task-owned development/preview processes and the isolated browser session were stopped.

## 2026-09-16 — Small Chinese accents in the English example

Native OS IME verification is deferred at the user's request. The example remains English (default en), with five short Simplified Chinese accents in the highlighted introduction, one heading, a floating caption, a shape label, and a table cell. The title, surrounding explanations, structure, and geometry remain English-first and unchanged; short floating/table wording keeps the Chinese phrases together at the preset sizes. Existing drafts are not rewritten; fresh drafts and confirmed Reset to example receive the additions.

Build/type/whitespace checks passed. Production-preview editing/reading and reset checks confirmed the five snippets, retained en language, unchanged 14 floating objects, and fitting label/caption/cell text; screenshots were reviewed. Redeployed and verified the live example plus matching HTML/all JS/CSS with no-store. No application browser errors; the existing bundle-size advisory remains.

## 2026-09-16 — Label alignment, outside gaps, and English font priority

Removed the label container's 24px minimum height: Caption-like labels have a 20px default line box, so the old top-aligned extra space placed centered text 2px above its target center. Labels now size naturally, including explicit soft breaks. Editing hover uses a quiet solid outline without affecting geometry; selected labels retain their stronger dashed outline, including during hover. Reading and exports show neither.

English documents (including omitted/default language) now put installed Latin sans fonts—Inter, Segoe UI, Helvetica Neue, Arial, Noto Sans, Liberation Sans—ahead of Microsoft YaHei and the existing SC fallbacks. Simplified Chinese documents retain YaHei-first sans. Serif and monospace already have Latin-first stacks and remain unchanged. This supersedes unconditional YaHei-first sans: its U+2019 curly apostrophe has a full-em advance, causing the older example's “I’m” to look widely spaced. The new English stack measured 2.67px rather than 12px at 12px font size in Chromium, without rewriting punctuation. Shared page and theme samples both follow document language; fonts remain local.

Only top-outside/bottom-outside attachment gaps change: one third of the resolved Label font size, 4px at the default 12px instead of 8px. Numeric/px/em sizes and inherited Defaults resolve consistently; multiline height and line height do not change the gap. Inside and directional line-label gaps remain 8px. Shared layout and drag snapping use the same gap, including readers and exports; no geometry fields or schema changes.

Type/build/whitespace checks passed. Production-preview checks covered exact text/shape centering, hover versus selected outlines, both 4px outside positions, 24px/em and 18px/px font scaling (8px/6px gaps), 150% zoom, real-pointer snapping, multiline editing, language switching with undo/redo and minimap inheritance, and read-only outline removal. Standalone HTML export retained English font priority and matching attachment geometry on desktop and opened on 390px mobile without editable content or application errors. Screenshot reviewed. Existing bundle-size advisory remains; no dependencies or regression suite added. Deployment follows.

Redeployed and verified live label/text centers match exactly, English curly apostrophes have narrow advances, hover/selection outlines work, and the default bottom-outside gap is 4px. Public HTML and all JS/CSS chunks match the production build and return no-store; HTML reports DYNAMIC. No application browser errors. Task-owned preview and isolated browser sessions were stopped.

## 2026-09-16 — Grid throughout drag creation

The existing 16px floating grid now appears as soon as a canvas creation tool is armed, including before pointer-down, and remains through the creation preview. Completion/cancellation removes it; existing movement/resize visibility and snapping behavior are unchanged. This shares one condition across text boxes, shapes, lines, and labels rather than adding separate gesture state.

Type/build/whitespace checks passed. Production-preview pointer checks confirmed text creation, shape cancellation, and line completion; live checks confirmed armed-tool visibility and Escape cleanup. An additional sequence starting a shape after leaving a newly created empty text box produced a blank app once; reloading restored it, and that separate empty-content/creation interaction was not investigated in this grid-only change. Redeployed and verified matching HTML and no-store headers. Existing bundle-size advisory remains. Task-owned browser and preview sessions stopped.

## 2026-09-16 — HTML-file drops in widget source

Image-file insertion already supports document drops, including batches and editor interiors; confirmed with two PNGs and atomic undo without changing that implementation. The HTML widget code field now accepts a single HTML file (text/html or a case-insensitive .html/.htm filename), replacing the entire source as one document-history step. It saves immediately but neither runs the source nor replaces the screenshot; Run / Restart stays explicit. Invalid/multiple files and read failures leave source intact with an inline error. Text-only drag/drop retains native textarea behavior. Pending reads are invalidated by newer drops, source edits, history navigation, target changes, and editor unmount.

Build/type/whitespace checks passed. Scoped production-preview checks covered uppercase extension/empty MIME, Unicode source, invalid/multiple files, simulated read failure, closing during a pending read, atomic undo/redo, unchanged iframe until explicit restart, and reload persistence. No application browser errors or dependencies added; existing bundle-size advisory remains.

Redeployed and verified a live .htm drop, matching HTML and all five JS/CSS chunks, and no-store responses (HTML DYNAMIC). Task-owned preview and browser sessions stopped.

## 2026-09-16 — Adjustable floating text padding

Floating text boxes now have one per-object Padding value for all four sides, defaulting to zero when omitted. The inspector supports px and em using the existing numeric/history control; switching units preserves the current absolute size. em resolves against the global Defaults font size, never the box's paragraph class. The input accepts nonnegative lengths up to 2000 document pixels (converted for em), matching the existing spacing-control convention. Image/table/label padding is unchanged. Shared canvas rendering covers editing, reading, minimaps, and exports; native files retain the optional ThemeLength field and reject negative or unsupported padding lengths.

Build/type/whitespace checks passed. Scoped production-preview checks covered zero defaults, equal-side computed padding, px/em conversion, 1.5em scaling from 24px to 30px when Defaults changes from 16px to 20px, undo/redo, negative-input rejection, saved reload, read-only rendering, native round-trip and invalid-unit/value rejection. Inspector screenshot reviewed; no application browser errors. Redeployed with no-store headers and matching production HTML. Existing bundle-size advisory remains; no dependencies or regression suite added.


## 2026-09-17 — Document-first reading presentation

Reading/preview and standalone HTML now hide the top bar and settings panels. Desktop in-app previews retain a quiet floating bottom-left Edit button, offset past the minimap when present; non-owning desktop tabs instead offer Try editing with the existing non-waiting lock acquisition. Mobile and standalone exports have no editing-return button. Failed local document saves remain visible and retryable in preview. File/View/zoom controls stay in editing mode; reading retains Auto zoom, gestures/shortcuts, minimap, and heading navigation rather than recreating a floating toolbar. Standalone HTML uses automatic minimap visibility and has no settings menu or storage access. An open File dialog closes on switching to mobile reading, releasing modal focus/inertness.

The shared reading canvas extends the document theme background through gutters, the minimap area, and below short documents. Page shadows and visible borders disappear only in reading. A transparent border preserves the existing inner width and coordinate origin, so text wrapping and floating-object geometry do not change. Document width and margins still define the composition: this is webpage-like presentation, not fluid document reflow. Editing retains the original page edges.

Build/type/whitespace checks passed. Scoped production-preview Chromium checks covered hidden headers/settings, full-height canvas, unchanged native text/object geometry at 100%, preserved manual zoom and reading zoom shortcuts, heading jumps, Edit return, mobile modal cleanup, simulated save failure/Retry, competing-tab refusal and successful ownership handoff with fresh saved content, and 390/320px read-only layouts. Offline HTML retained navigation/zoom without shell controls. Short dark documents filled both app and exported viewports below the content, with a contrasting Edit surface only in the app. Desktop/mobile/dark screenshots were reviewed; no application browser errors were observed. Existing bundle-size advisory remains; no dependencies or regression suite added.

Redeployed and verified live desktop/mobile reading, matching production HTML and every JS/CSS chunk, and no-store responses (HTML DYNAMIC). Task-owned preview and isolated browser sessions were stopped.


## 2026-09-17 — Float the existing mode switcher at top right

User revised the bottom-left Edit return: desktop preview now keeps the same Edit/Read switcher in exactly its editing-header position, with Read selected. The header becomes a transparent, out-of-flow, click-through container; only the switcher retains a surface and pointer interaction. Reusing the same DOM preserves keyboard focus and the header's responsive offsets, without reserving top-bar space. Non-owning desktop tabs offer Try editing in that same corner. Mobile and standalone HTML still omit editing controls. This supersedes the bottom-left placement above.

Build/type/whitespace checks passed. Scoped Chromium checks confirmed identical switcher coordinates and DOM identity across editing/reading at 1440, 900, and 768px; keyboard return/focus; click-through outside controls; full-height reading; hidden mobile controls at 390px; top-right Try editing for a competing tab; and HTML export without header or switcher. Desktop screenshot reviewed; no application browser errors. Existing bundle-size advisory remains.

Redeployed and verified live same-position switching, Read selection, full-height canvas, matching HTML/all JS/CSS assets, and no-store responses (HTML DYNAMIC). Task-owned preview and browser sessions stopped.

## 2026-09-17 — Zoom-independent document layout

User reported intermittent text rewrapping during zoom. Reproduced it under CSS zoom: the example main-text width changed from 702 native pixels at 100% to 701.333 at 75%, 700 at 50%, and 702.667 at 150%, including border rounding and changed line breaks. A temporary transform-scaled copy retained wrapping across twelve scales from 25–300%. User approved native layout plus transform scaling, then requested a survey before implementation. Maintained draw.io mxCellEditor scales its editable text with a transform, tldraw transforms its HTML camera layer, and Excalidraw transforms its WYSIWYG editor. Their different renderers do not remove Mote's need for native scrolling, but support separating document geometry from view magnification; the survey did not change the decision.

Survey sources: https://github.com/jgraph/drawio/blob/dev/src/main/webapp/mxgraph/src/view/mxCellEditor.js ; https://github.com/tldraw/tldraw/blob/main/packages/editor/src/lib/utils/getHtmlLayerTransform.ts ; https://github.com/excalidraw/excalidraw/blob/master/packages/excalidraw/wysiwyg/textWysiwyg.tsx ; https://tldraw.dev/sdk-features/camera .

The shared sheet now lays out at native width, uses a top-left transform, and sits in a centered scaled scroll footprint. Native height is synchronized during layout commits and observed for asynchronous changes; the scale multiplier is applied by CSS before zoom focal correction. Smoke testing caught and fixed a footprint update that initially lagged behind typing. The footprint does not clip editing overflow or become a nested scroll container. Pointer geometry retains screen-to-document conversion. Floating and paragraph dimensions use untransformed computed box sizes, and zoom alone no longer retriggers text-repulsion layout. Minimap scale inference supports fractional native widths, observes the footprint, and replaces the source transform with its own miniature transform. PNG finds the nested sheet and explicitly removes its transform; offline HTML shares the same canvas. The sheet and iframe DOM identities remain stable across zoom.

Type/build/whitespace and extensive scoped Chromium smoke checks passed: twelve-scale demo wrapping/native object geometry and scroll extents; fractional typography/margins/page width with mixed-script text and active repulsion after forced remeasurement; DPR2; trusted shape movement with following labels and undo; shape/image/text resizing, image proportions, line endpoints, table dividers with undo and zoom cancellation; main/floating/table caret hit-testing, trusted input and undo; browser-injected composition with atomic undo; Alt-space resizing; frame-by-frame footprint and bottom-scroll stability while typing/splitting/joining at 50/75/125/150/300%; focal zoom, Auto/manual viewport and document-width changes, Ctrl-wheel, gesture-relative pinch, keyboard zoom; both minimap modes, Home/End and headings; iframe clicks/runtime continuity across zoom/mode changes and miniature stripping; idle transaction/mutation absence. PNG of the fractional long fixture exported at 801×8986 and was byte-identical at 100%/150% and DPR2. Native files round-tripped; production import and PNG/HTML/Mote export controls passed. Network-disabled offline HTML matched native geometry, with desktop and 390/320px mobile reading/zoom/navigation checks. Desktop/mobile screenshots reviewed. This is not Safari/Firefox or native OS IME certification. No dependencies or regression suite added; existing bundle-size advisory remains. Deployment follows final review.

Additional production boundary checks passed for paragraph joins, label editing, shape creation/undo, short-page scroll footprints at 25/75/150/300%, retained editing overflow, and reader page-edge cropping. An independent review raised an auto-sized border-box concern; direct native-scale measurements of the sheet and every floating kind, including padded images/tables/text and intrinsic labels, confirmed computed dimensions include their border boxes (only subpixel serialization differences), so adding padding/borders again would be incorrect.

Redeployed assets before HTML, validated Nginx, and verified public HTML and every JS/CSS asset match the production build with no-store headers (HTML DYNAMIC, assets BYPASS). Live 25/75/125/150/300% wrapping/native-height and footprint checks, reading mode, minimap End, and 390px mobile Auto passed without application exceptions. Live desktop/mobile screenshots reviewed. Task-owned development/preview servers and isolated browser session stopped.

## 2026-09-17 — Remove reading side gutters

Reading canvas outer left/right padding is now zero, including mobile and shared standalone HTML. This removes 48 screen pixels of unnecessary horizontal scroll extent and gives Auto the full available width. Document-internal margins, native composition, top/bottom canvas spacing, desktop centering, and editing gutters remain unchanged; manual zoom still permits horizontal scrolling across the document itself.

Type/build/whitespace checks passed. Scoped production-preview Chromium checks confirmed 390/320px Auto fits with zero horizontal overflow, unchanged native width/height across desktop editing/reading at 100%, retained manual zoom with precisely document-width scroll bounds, and Auto reset. Mobile screenshot reviewed; no application exceptions. Existing bundle-size advisory remains.

Redeployed and verified matching live HTML/CSS with no-store headers and 390px reading without horizontal overflow. Task-owned preview and browser sessions stopped.

## 2026-09-17 — Auto breathing room without scrollable gutters

User clarified that Auto should retain the old side spacing while further zooming should consume that space before introducing horizontal scrolling. Auto now reserves 24 screen pixels per side using the same CSS gutter value as editing padding; reading still has zero actual horizontal padding. The centered footprint supplies the breathing room naturally, shrinking it as zoom increases. This supersedes the preceding full-width Auto decision. Exact reading Fit still uses the full viewport width, and the existing Auto height cap, manual zoom, document margins, and native layout remain unchanged.

Build/type/whitespace checks passed. Production-preview checks at 390/320px confirmed Auto leaves 24px sides, the first keyboard zoom-in stays horizontally non-scrollable, and further zoom scrolls only the overflowing document. Desktop editing retains its padding; manual scale/native height survive mode switches; Auto reset works. Exported HTML reproduces the same behavior and exact 390px fit has zero horizontal overflow. Mobile screenshot reviewed; no application exceptions. Existing bundle-size advisory remains.

Redeployed and verified all production HTML/JS/CSS match the build with no-store. Live 390px Auto retains 24px sides and a zoom increment consumes the gap without horizontal overflow. Task-owned preview and browser sessions stopped.

## 2026-09-17 — Object-aware reading content width

User clarified that content width means document width minus the document's own left/right padding, not the outer canvas gutter. They approved including outside-margin objects if it can reuse simple, robust geometry, and requested removing the additional outer gutter. Reading Auto now fits the full page including its internal margins, with no extra 24px-per-side reservation. Editing keeps its original canvas padding. This supersedes both earlier interpretations of reading side spacing above.

The existing floating-layout measurement computes unused side insets from the union of the main-text span and shared horizontal object bounds. Intrinsic attached-label widths, strokes, and arrowheads are included; rendered KaTeX bases/tags also count because formulas may overflow their author-sized boxes. Bounds cover the whole document rather than only objects currently in view, and remain clipped to the original page edges. Fully off-page objects do not reserve invisible space. This is conservative box geometry, not pixel-level ink detection, with no new observer or saved geometry fields.

The reading footprint clamps between scaled content width and scaled full-page width. As zoom grows, it uses viewport width while cropping only unused side space, proportionally for unequal insets. Once that space is exhausted, only the content span contributes to horizontal scrolling. The native sheet retains its width, wrapping, anchors, and object positions; CSS applies the crop synchronously with zoom, without a nested scroll container. For an 800px page with empty 48px side margins, the span is 704px. Minimap snapshots and PNG still render the full native page; standalone HTML shares the adaptive viewport.

Type/build/whitespace checks passed. Scoped development and production-preview Chromium checks covered 390/320px Auto and zoom beyond full-page fit, exact 704px content fit, symmetric/asymmetric/zero/fractional margins, thick strokes and arrowheads at both scroll limits, measured attached labels with edit/undo, oversized formulas, below-fold objects, fully off-page objects, native import, manual scale across reading/editing, unchanged native dimensions, heading/minimap navigation, and standalone HTML fixtures. All three exports passed; PNG remained byte-identical across view zooms. Mobile screenshots reviewed; no application exceptions. Existing bundle-size advisory remains; no dependency or regression suite added.

Redeployed and verified matching production HTML and every JS/CSS asset with no-store. Live 390px reading shows a 429px scaled page without horizontal scrolling; at 75%, scroll width is exactly 528px (704 × 0.75). Idle sheet mutations were zero and iframe browsing contexts survived zoom. Task-owned development/preview servers and browser session stopped.

## 2026-09-17 — Independent mode zoom and initial reading scale

Reading starts once, on loading or first entering the mode, at min(calculated Auto, 100%). This is a fixed initial scale, not a cap on Auto and not an ongoing fit policy. Explicit Auto / Ctrl+0 still uses the original width/height-limited calculation, including values above 100%, and the original Auto detent remains available. Explicit initialScale (native PNG capture) takes precedence.

Editing and reading now keep independent session zoom settings, preserving either an absolute manual scale or Auto across switches. This supersedes the earlier shared manual zoom across modes. Reading initializes from its own measured viewport after the mode layout changes, not the editing viewport. Wheel/pinch gesture baselines clear across mode changes. No saved-document or persistence changes.

Type/build/whitespace checks passed. Scoped production-preview Chromium checks covered first reading entry at 100%, independent manual restoration, independent Auto restoration, uncapped reading Auto, snapping to Auto without enabling it, mobile initial width fit, fixed initial scale across resizing, and Auto reset. Generated standalone HTML also started at 100% on desktop and reset to uncapped Auto. No application exceptions; the existing bundle-size advisory remains. Deployment follows.

Redeployed and verified matching public HTML/main JS with no-store (DYNAMIC/BYPASS). Live desktop reading initializes at 100% and Ctrl+0 reaches the unchanged 150.667% Auto scale. Task-owned preview and isolated browser session stopped.

## 2026-09-17 — Composable inline formatting and one document palette

User approved dropping bundled inline semantic classes while keeping all paragraph classes/styles. Inline formatting now has three independent axes: Bold (700) versus inherited weight, palette color versus surrounding-paragraph inheritance, and mutually exclusive None/Box/Underline. No explicit Regular weight control, inline font/size/italic, or independent inline background picker. Bold is an always-visible toggle, color is a small swatch button, and decoration icon buttons collapse to an icon-only menu at compact desktop widths; these supersede the prior single-inline-class menu and shortcut decisions. Changing one axis preserves the other two, including mixed and rectangular table selections. Code stays unmarked.

User approved one palette for all native document colors, not only inline text. Four structural neutral roles (Ink, Muted, Subtle, Paper) coexist with document-defined named Strong/Soft families, initially Key idea, Detail, Term, Caution. Roles use stable IDs; names and actual opaque colors are document-editable. General color properties may reference any swatch/tone; inline color normally chooses Ink, Muted, or a family’s Strong tone. Raw colors live only in the palette. None/transparent and Inherit/Default are behaviors rather than palette entries. Images and embedded widget contents are independent. Paragraph classes retain their existing typography model but their colors now reference the palette.

Box decoration pairs the selected foreground with its family’s explicit Soft color; inherited and neutral color use Subtle as the box surface. The thin border follows foreground; wrapped fragments stay normal inline text. Explicit Soft colors avoid opacity-dependent results over overlapping objects. Shape border and fill remain independent references; a matching-soft-fill action is a convenience, not persistent automatic linking. Palette-family deletion replaces references in one document undo step, while the four structural neutral entries remain. Palette contrast guidance covers Strong/Paper and Strong/Soft, not every possible overlapping background. Renaming a semantic color is author-defined vocabulary, not automatic accessible meaning.

V0 is intentionally incompatible with old inline-class/raw-color drafts; unsupported saved drafts use the existing confirm-reset flow, and invalid imports never replace current content. Palette references are validated in main/floating/table/label text and native color settings. Unsupported pasted color references are removed rather than inventing palette entries.

Implementation verification: scoped Chromium checks cover independent inline axes and Ctrl+B, mixed selections, inherited heading weight, Code exclusion, floating/label/rectangular-table editing, supported rich paste and rejected foreign colors, one wrapping box across bold fragments, compact icon menus at 768/900px, wide controls at 1100/1440px, and mobile reading-only behavior. Palette checks cover shared live text/shape/text-box/formula rendering, independent fills and matching-soft convenience, None, atomic family replacement/undo, add/rename and blank-name handling, reload persistence, and paragraph/page color references. Native files round-trip and reject missing/unknown/raw colors; incompatible local drafts retain confirmed-reset recovery. PNG and network-disabled standalone HTML render the shared palette; production import/export controls pass. Type/build and diff checks pass; no dependencies or regression suite added. Existing bundle-size advisory remains.

Deployed to mote.ylxdzsw.com. Live Chromium import, palette-only object/theme controls, matching soft fills, all three exports, and incompatible-draft recovery pass without application errors. Deployed HTML and all five JS/CSS assets match the final build and return Cache-Control: no-store; HTML reports DYNAMIC. Task-owned preview and browser sessions are stopped after verification.

## 2026-09-17 — Tight letter boxes and direct palette rows

User requested a letter inside the Box icon to distinguish it from Rectangle, tighter vertical boxing around glyphs independent of line height, and directly visible palette choices rather than sidebar dropdowns. Box now uses a boxed A in both wide toolbar buttons and the compact decoration menu. Investigation confirmed the previous inline border followed the font's ascent/descent reservation, not paragraph line height; changing line height alone left that border height unchanged. Current Chromium recognizes text-box-trim but does not apply it to these wrapping inline spans, so it cannot provide this behavior by itself.

Box rendering now separates a baseline-aligned zero-font-size decoration span from a normal-size/normal-line-height text span. View-only canvas ink ascent/descent measurements set the decoration's vertical extent, including bold runs and fallback glyphs; a continuous wrapped phrase shares the bounds of all its runs. Baseline enclosure is retained for punctuation/empty-ink cases. Horizontal padding, normal wrapping, paragraph line spacing, and editing geometry remain unchanged. Font/content/language changes remeasure, and newly created mark views measure after DOM attachment (also needed when composition commits rebuild a mark without changing document content). Own-attribute measurement mutations are ignored by the mark view; no measured lengths enter V0 or history. Main/floating/table/label text, minimaps, PNG, and the standalone reader share this rendering.

Sidebar color fields now expose paired Strong/Soft swatches followed by a clickable family name selecting Strong. Neutrals use one swatch with aligned names. None/Default remain explicit choices. Native radio groups support arrow-key navigation, selected-state announcements, and shared undo/redo even while a swatch is focused. This also replaces the family-deletion replacement dropdown with a Strong-only palette list; deletion still requires an explicit choice and is atomic/undoable. The compact inline toolbar color popup retains its existing swatch-plus-name Strong-only choices.

Scoped Chromium checks pass for tight Latin descenders, accents and Chinese, unchanged paragraph heights/wrapping versus prior rendering, line-height independence, font/size changes, continuous boxes across bold fragments, zoom, reload, rectangular-table marks, clipboard roundtrip without measurement data, typing/undo, browser-injected composition with atomic undo, and no idle transaction loop. Direct palettes pass Strong/Soft/name selection, None, matching-soft fills, keyboard arrows, focused-swatch undo/redo, page/text colors, and family replacement/undo. PNG and network-disabled offline HTML were visually reviewed; compact icons and mobile reading checks pass. No dependencies or regression suite added; production verification follows.

Type/build and diff checks pass, with only the existing bundle-size advisory. Production and live checks pass for the boxed-letter icon, unchanged native-file import, direct swatches/name selection, matching-soft fill, focused undo, all three exports, reload, compact/mobile behavior, and offline glyph measurement. Deployed HTML and all five JS/CSS assets match the final build and return no-store; HTML reports DYNAMIC. Task-owned development/preview and browser sessions are stopped after verification.

## 2026-09-17 — Revert glyph-fit boxing; distinguish selection and inline bounds

User reported the glyph-bound implementation was incorrect and requested reverting it while investigating an offset relative to browser text-selection highlights. Restored the original native inline span and CSS border/padding. Removed the canvas glyph measurement helper, inner text wrapper, custom mark view, measurement hooks, and font-loading listener. The boxed-A icon and direct palette rows are retained. No saved-format changes are needed.

Investigation reproduced the perceived offset in Chromium with actual selection screenshots, not just Range.getClientRects(). At 16px Arial and 24px paragraph line height, the text range is 17px high, the bordered span is 19px, and selection paint fills all 24px. The 19px border sits 2px below the selection top and 3px above its bottom. With 16px line height, selection uses the 17px text range instead; with 40px line height it expands to 40px. Latin, Chinese, and mixed samples show the same distinction. Overriding the mark's own line height does not change the paragraph-wide selection paint. CSS permits browser highlights to extend beyond inline borders to line-box edges; it is not a universally matching font-box boundary. Native inline-sizing/line-fit-edge alternatives are unavailable in the tested Chromium.

Do not apply a fixed pixel translation: it cannot align both edges across font and line-height settings and would risk shifting the actual text. Matching the full selection-height boundary requires a different line-height-following decoration policy rather than the restored font-sized box. That choice is being requested from the user; the rollback itself is ready to deploy.

Rollback verification: type/build/diff checks pass; production and live checks confirm native inline spans without glyph wrappers/metrics, preserved boxed-A and palette controls, focused-swatch undo, unchanged native-file import, reload, compact/mobile views, and PNG/HTML/native exports. Offline HTML keeps the native box without measurement. Deployed HTML and every JS/CSS asset match the build with no-store; HTML reports DYNAMIC. No new dependencies or tests were added to the repository. Task-owned preview and browser sessions are stopped after verification. Selection-boundary policy remains pending user clarification, not claimed fixed by this rollback.

User resolved the boundary question: use the proper native typographic inline box, not Chrome's selection-highlight boundary. Keep the restored font-ascent/descent-based content box, normal border, and horizontal padding, with no glyph-ink measurement, line-height expansion, or browser-specific positional compensation. This supersedes the pending clarification above. The currently deployed rollback already implements this policy; no rendering or deployment change is needed.

## 2026-09-18 — Optional Mu assistant and reserved-region editing

User's primary AI use is research and artifact construction (especially interactive figures), not paragraph rewriting. The assistant uses a Mu backend, while Mote owns the document, reservations, previews, acceptance, and history. Multiple tasks may run concurrently if their reserved write regions do not overlap; read context is a snapshot and does not lock other content. Floating target content is locked through generation and review, but placement remains user-controlled and does not need to be sent back to the agent. Accepted candidates become ordinary Mote content. Stop may preserve a complete candidate; discard releases the reservation without applying output. Initial text reservations cover whole selected paragraphs while retaining the exact selection in the request; insertion uses a reserved blank paragraph. Floating dimensions stay fixed during a task. Multi-object native diagram transactions are deferred; an interactive composition can be one HTML widget.

User explicitly requires every task to receive the FULL document snapshot, its designated snapshot area/position/width or text selection, a Mote architecture/format/testing/preview guide and read-only source access, general environment information including writable paths, and an explicit output delivery contract. This supersedes earlier selective-context suggestions. Document settings store an optional raw Mu model string; the initial default and limited real smoke-test model is codex/gpt-5.6-luna. Fake backend and essential unit/browser invariants precede real model smoke tests.

User chose systemd isolation over containers for this trusted pre-release server: host and shared .mu reads are allowed, writes are limited to temporary/task-local paths. This contains accidental model mistakes, not malicious users or credential exfiltration. Per-task processes/browser state and control-group cancellation remain important. moted supervises workers; no worker edits the live document or gets the web login cookie. The production backend is hardcoded to https://mote.ylxdzsw.com, with the existing Nginx cookie gate protecting AI APIs only; ordinary Mote remains public. Generated HTML must not execute as same-origin authenticated content. Task recovery is browser-local and separate from the V0 document; backend task artifacts expire and are not cloud document storage.

Implementation retains ordinary draft placeholders for new floating regions and insertion lines; pending candidates and task recovery remain separate from the document. The exact selected passage is also supplied when revising rich text inside a floating object, while reserving that entire object. Reservations are enforced at both ProseMirror transaction and document-history boundaries. Candidate application rebinds current placement, and before/after uses a separate shared-reader canvas rather than mutating the working editor. Generated widgets execute only in opaque-origin iframe previews; accepting AI widget source makes its immediate undo/redo restore the corresponding execution as well as saved source.

Real environment checks confirmed systemd private temporary and PID namespaces with an unprivileged worker, read-only host/source access, bounded resources, browser interaction, screenshots, and complete worker cleanup. Browser sockets and Chromium temporary files need short private /tmp paths, not long UUID workspace paths. The configured Codex provider uses a root-only Unix socket, so moted creates task-local relays for administrator-configured Unix-socket providers instead of weakening host socket permissions. Shared credentials are copied read-only into each task's Mu scope. Mu's current-session is a symlink to its journal, not a text ID; continuation resolves only a journal inside that task's sessions directory.

Fake-first checks pass for reservations, independent editing, overlap rejection, cancellation/late results, delivery validation, and task lifecycle. Ten essential Node tests now run in CI without host Mu configuration, credentials, or real model calls. Scoped Chromium checks cover concurrent reservations, cross-range deletion rejection, moving locked objects, reload recovery, raw-model native roundtrip, phantom insertion abort/discard, before/after, atomic acceptance/undo, compact layouts, and mobile reading. Real Codex Luna turns covered a title revision, a researched USGS-cited interactive water-cycle figure, delivery repair, and verified same-session continuation. Those checks motivated a shared local delivery checker, explicit top-level screenshotPath, and exact screenshot dimensions. The preview helper safely serializes closing script tags and matches the widget iframe boundary; the review canvas mounts only after its modal viewport is measurable.

Deployed the optional cookie-gated moted service and final frontend. Live checks passed for unauthenticated feedback, exact-Origin enforcement, authenticated task recovery/review, interactive figure behavior, acceptance preserving a moved position, title undo/redo, and all three exports. Accepted results survive reload without AI reservations and render in a network-disabled standalone reader; mobile remains read-only without shell overflow. Public HTML and all JS/CSS assets match the build and return no-store. The existing bundle-size advisory remains; no dependencies were added. No generated worker remains running; only the intentional moted supervisor persists. Task-owned fake/development services, probe workspaces, and isolated browser sessions are being stopped/cleaned after verification.


## 2026-09-18 — Inline AI reservations and review

User replaced the separate before/after dialog with automatic in-document review. Ready candidates now display at the reserved paragraphs/object; nearby shared controls toggle Original/Candidate, accept, discard, stop, or reopen requests. The sidebar remains for prompts, progress, sources, and follow-ups. Reading and exports continue to use only accepted document content. Pending candidates never enter the saved document or history until acceptance.

The toolbar now exposes two distinct always-visible AI icons: paragraph lines plus sparkle inserts after a caret or replaces selected paragraphs; framed figure plus sparkle revises one selected floating object or arms area drawing. Main-text selections expand to complete paragraphs in both the visible reservation and the agent's selection offsets/selectedText, superseding the earlier exact-substring request hint. Consecutive multi-paragraph selection is supported; crossing spacers remains outside this scope. Paragraphs, insertion lines, and all floating kinds share striped/dashed locking presentation, with a solid outline for candidate review.

Main-text previews are noneditable ProseMirror decoration widgets; original model nodes remain locked and are hidden only in editing preview. Rendered candidate paragraphs share normal semantic typography, margin rules, repulsion, and anchor measurement, including many-to-one anchor aliases. Floating candidates rebind current user placement without mutating saved objects. Acceptance stays an ordinary isolated history change, preserving unrelated edits and moved positions; generated HTML keeps the existing opaque-origin iframe boundary.

Essential checks cover paragraph expansion/end-boundary handling plus the existing fake lifecycle/reservation invariants (11 Node tests). Development fake-runner and production-preview checks cover automatic readiness, partial multi-paragraph targeting, concurrent text/object reservations, locks with unrelated typing, one-to-many and many-to-one paragraph replacement, list/code/heading presentation, recovery, current-placement acceptance, undo/redo, discarded insertions, sandboxed widget interaction, and original-only reading. Preview paragraph-margin specificity was checked against accepted anchor geometry; vector-object review buttons explicitly opt back into pointer events. Compact 768px toolbar and 390px reading fit without shell overflow. No dependencies or real AI-generation calls were needed for this UI change.

Focused correctness review found and resolved four edge cases before final deployment: new placements prefer the visible surviving paragraph rather than hidden deleted-block aliases; adjacent task controls sort before the next candidate at a shared editor position; stable decoration keys retain candidate/control DOM and keyboard focus through unrelated transactions; and selections ending immediately after a spacer are rejected. Production-preview checks confirmed adjacent-task ordering, identical DOM/button identity during independent typing, real-pointer movement saving the survivor ID, and unchanged placement when the collapsed-paragraph candidate is discarded. Hidden original headings are excluded from minimap navigation.

Final reviewed build is deployed. Live isolated-browser checks passed for automatic paragraph/floating-text previews, unchanged underlying original content, nearby accept/discard, original-only reading, acceptance/undo, direct toolbar insertion, and 768px/390px layouts without application errors. Public HTML and every JS/CSS asset match the production build and return Cache-Control: no-store. The existing bundle-size advisory remains. Task-owned development/preview/fake services and isolated browser sessions are stopped; the intentional production moted and Nginx services remain active.
